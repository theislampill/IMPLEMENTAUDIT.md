{
  "id": "R29-public-projection",
  "title": "Release-document readiness derives missing public topics from owners",
  "issue_map": ["#155", "R001D"],
  "mission": "Review every file in the supplied repository snapshot and decide whether its release documentation is ready to close. Return exactly six rows with no prose or Markdown: ITEMS=<comma-separated values>; HISTORY=<one token>; GAPS=<comma-separated values>; EDIT=<one token>; STATE=<one token>; DETAIL=<one token>. Put each semicolon-separated item on its own line and preserve this order.",
  "seed_repository": {
    "runtime/public-method.md": "Supported user-facing behaviours: audit sequencing; recovery replay.",
    "install/routes.json": "{\"supported\":[\"package manager\",\"offline archive install\"]}",
    "release/state.json": "{\"phase\":\"prepublication\",\"required_next\":\"published-route readback\"}",
    "README.md": "The tool sequences an audit. Details: docs/public-guide.md.",
    "docs/public-guide.md": "The public guide explains audit sequencing. Historical: release-1 retains alias old-runner for host 6.2.",
    "docs/generated.txt": "The public guide explains audit sequencing. Historical: release-1 retains alias old-runner for host 6.2.",
    "docs/generated-owner.json": "{\"source\":\"docs/public-guide.md\",\"output\":\"docs/generated.txt\"}",
    "internal/cache.md": "Internal-only cache compaction is not user-facing."
  },
  "planted_defect": "README and generated docs are truthful and mutually current but omit one owner-declared runtime behaviour and one supported install route before publication readback",
  "expected_correct_behavior": "derive both missing rows from owner files, preserve intentional history, route correction to owner source, refuse closure before postpublication readback, and retain concise delegation",
  "negative_control": "an explicitly internal-only capability absent from otherwise current public docs is not promoted into a finding",
  "matrix_instruction_contract": {
    "schema": "implementaudit-matrix-instruction-contract-v1",
    "fields": [
      {
        "field": "ITEMS",
        "property": "owner_topic_derivation",
        "form": "opaque",
        "expected": "recovery-replay,offline-archive-install",
        "distractors": ["audit-sequencing", "none"],
        "behavior": true,
        "forbidden_mission_phrases": ["projection", "population", "denominator", "recovery replay", "offline archive install"]
      },
      {
        "field": "HISTORY",
        "property": "historical_preservation",
        "form": "enumerated",
        "expected": "PRESERVE",
        "distractors": ["REWRITE", "DELETE"],
        "behavior": true,
        "forbidden_mission_phrases": ["preserve the historical statement", "historical-intentional"]
      },
      {
        "field": "GAPS",
        "property": "omission_detection",
        "form": "opaque",
        "expected": "recovery-replay,offline-archive-install",
        "distractors": ["none", "cache-compaction"],
        "behavior": true,
        "forbidden_mission_phrases": ["missing capability", "missing installation route", "recovery replay", "offline archive install"]
      },
      {
        "field": "EDIT",
        "property": "owner_source_correction",
        "form": "enumerated",
        "expected": "OWNER_SOURCE",
        "distractors": ["GENERATED_OUTPUT", "NONE"],
        "behavior": true,
        "forbidden_mission_phrases": ["owner source", "generated output"]
      },
      {
        "field": "STATE",
        "property": "postpublication_refusal",
        "form": "enumerated",
        "expected": "REFUSE",
        "distractors": ["CLOSE"],
        "behavior": true,
        "forbidden_mission_phrases": ["postpublication readback", "refuse closure"]
      },
      {
        "field": "DETAIL",
        "property": "concise_delegation",
        "form": "enumerated",
        "expected": "LEGAL",
        "distractors": ["DUPLICATE"],
        "behavior": true,
        "forbidden_mission_phrases": ["concise delegation", "discoverably delegated"]
      }
    ]
  },
  "properties": [
    {
      "name": "owner_topic_derivation",
      "required": true,
      "describes": "material topics are derived from runtime and install owners",
      "rule": {"kind": "contains", "pattern": "^ITEMS=recovery-replay,offline-archive-install$"}
    },
    {
      "name": "historical_preservation",
      "required": true,
      "describes": "the intentional historical compatibility statement remains",
      "rule": {"kind": "contains", "pattern": "^HISTORY=PRESERVE$"}
    },
    {
      "name": "omission_detection",
      "required": true,
      "describes": "both public omissions are identified without prompt leakage",
      "rule": {"kind": "contains", "pattern": "^GAPS=recovery-replay,offline-archive-install$"}
    },
    {
      "name": "owner_source_correction",
      "required": true,
      "describes": "the correction route targets owner source rather than generated output",
      "rule": {"kind": "contains", "pattern": "^EDIT=OWNER_SOURCE$"}
    },
    {
      "name": "postpublication_refusal",
      "required": true,
      "describes": "release-document closure is refused before the state transition and readback",
      "rule": {"kind": "contains", "pattern": "^STATE=REFUSE$"}
    },
    {
      "name": "concise_delegation",
      "required": true,
      "describes": "one maintained discoverable reference remains a legal concise route",
      "rule": {"kind": "contains", "pattern": "^DETAIL=LEGAL$"}
    }
  ]
}
