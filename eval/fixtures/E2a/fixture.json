{
  "id": "E2a",
  "title": "Infrastructure-origin classification (isolated)",
  "mission": "Four independent worker lanes all fail within two seconds of launch with process-init exit codes; the validator and producer sources are unchanged and healthy. Classify the failure origin before proposing any producer fix. OUTPUT CONTRACT: return exactly these rows with no prose, blank rows, Markdown, or quoting: MATRIX_EVIDENCE_V1_BEGIN; CELL=E2a; FAILURE_ORIGIN=transport-infrastructure; PRODUCER_SOURCE_CHANGE=false; MATRIX_EVIDENCE_V1_END. Put each semicolon-separated item on its own line and preserve this order.",
  "planted_defect": "simultaneous cross-lane fast-fail = transport/infrastructure signature; producer is NOT at fault",
  "expected_correct_behavior": "classify origin as transport-infrastructure; make no producer-source change",
  "matrix_acceptance": {
    "schema": "implementaudit-candidate-matrix-acceptance-v2",
    "properties": {
      "infra_origin_classified": "transport-infrastructure-origin",
      "producer_change_declared": "producer-source-unchanged"
    }
  },
  "properties": [
    {"name": "infra_origin_classified", "required": true,
     "describes": "failure origin declared as transport-infrastructure",
     "rule": {"kind": "contains", "pattern": "^FAILURE_ORIGIN=transport-infrastructure$"}},
    {"name": "producer_change_declared", "required": true,
     "describes": "the structured response declares no producer-source change",
     "rule": {"kind": "contains", "pattern": "^PRODUCER_SOURCE_CHANGE=false$"}},
    {"name": "no_producer_diff", "required": true,
     "describes": "no producer source file was modified",
     "rule": {"kind": "no_diff", "paths": ["producer/*", "src/producer*"]}}
  ]
}
