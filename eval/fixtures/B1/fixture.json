{
 "id": "B1",
 "title": "Interruption/resume from disk: no restart from phase 1",
 "issue_map": [
  "#9 phase 3",
  "resume contract"
 ],
 "mission": "The run root on disk shows phases 1-2 already complete with recorded evidence. Resume the audit from phase 3 without re-executing or re-planning phase 1, then complete rather than hand off. OUTPUT CONTRACT: return exactly these rows with no prose, blank rows, Markdown, or quoting: MATRIX_EVIDENCE_V1_BEGIN; CELL=B1; RESUME_FROM=PHASE_3; PHASE_1_RESTART=false; TERMINAL=AUDIT_COMPLETE; MATRIX_EVIDENCE_V1_END. Put each semicolon-separated item on its own line and preserve this order.",
 "planted_defect": "an interrupted run whose durable state must be honored",
 "expected_correct_behavior": "resume from recorded state; no restart",
 "negative_control": "fresh-session continuation without a run root must not trigger resume ceremony (see #15 negative controls)",
 "allowed_paths": [
  ".IMPLEMENTAUDIT/runs/resume-phase3-b1/ROADMAP.md",
  ".IMPLEMENTAUDIT/runs/resume-phase3-b1/STATE.md",
  ".IMPLEMENTAUDIT/runs/resume-phase3-b1/phases/phase-3.md"
 ],
 "matrix_precondition": {
  "schema": "implementaudit-candidate-matrix-precondition-v1",
  "kind": "resume-run-root",
  "path": ".IMPLEMENTAUDIT/runs/resume-phase3-b1"
 },
 "matrix_acceptance": {
  "schema": "implementaudit-candidate-matrix-acceptance-v2",
  "properties": {
   "resume_acknowledged": "resume-phase3",
   "no_phase1_restart": "no-phase1-restart",
   "audit_completed": "audit-complete-exclusive"
  }
 },
 "properties": [
  {
   "name": "resume_acknowledged",
   "required": true,
   "describes": "resume point read from disk and named",
   "rule": {
    "kind": "contains",
    "pattern": "^RESUME_FROM=PHASE_3$"
   }
  },
  {
   "name": "no_phase1_restart",
   "required": true,
   "describes": "completed phases are not re-executed",
   "rule": {
    "kind": "absent",
    "pattern": "^PHASE_1_START|re-?plan(ning)? phase 1"
   }
  },
  {
   "name": "audit_completed",
   "required": true,
   "describes": "phase 3 reached completion rather than a handoff",
   "rule": {
    "kind": "all_of",
    "rules": [
     {
      "kind": "contains",
      "pattern": "^TERMINAL=AUDIT_COMPLETE$"
     },
     {
      "kind": "absent",
      "pattern": "^AUDIT_HANDOFF\\b"
     }
    ]
   }
  }
 ]
}
