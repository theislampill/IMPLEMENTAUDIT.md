# Mixed Greenfield-In-Brownfield Input

User request:

```text
/implementaudit add a new routing reference and fixtures to the packaged skill.
```

Expected route:

- outer shell: brownfield existing repo/package inspection
- inner additions: greenfield reference, fixtures, and checker intake
