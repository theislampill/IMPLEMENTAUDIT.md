test(eval): align the adversarial control

Finding: the acceptance envelope changed.
Countermeasure: align the static control with the current envelope.
Evidence: tests/commit-message-contract.test.sh
