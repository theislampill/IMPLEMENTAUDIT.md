# Protocol — exemplar run root

In a real dispatched run this file is a verbatim copy of
`templates/PROTOCOL.md` from the skill payload (dispatch-prep step 3 records
the source path). Kept as a pointer here so the tracked exemplar stays small.
