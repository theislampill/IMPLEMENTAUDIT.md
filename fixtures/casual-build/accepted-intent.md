# Fixture: casual-build intent accepted — normalized into audit object

This fixture documents how governed casual-build intake accepts natural-language
repo-build intent and normalizes it into a `tdqyq-audit-object` before any
mutation.

## Classification: governed casual-build intake

Input received: "Make the docs portal generated by CI and prove it is fresh."

## Input gate evaluation

The input is:
- A recognizable repo-build goal: yes
- Repo-specific (not empty, unsafe, or impossible): yes
- Actionable without bypassing audit gates: yes

Input gate: PASS — proceed to Stage 1 intake

## Stage 1 — Audit-governed intake

Restate target: Generate the docs portal via a CI-driven build script and
verify freshness with a check script.

Route classification: mixed (brownfield repo + greenfield portal scripts)

Batched intake questions (up to 4):
1. Should the portal deploy to GitHub Pages on push to main, or only build
   locally in CI for validation?
2. Are there existing HTML/docs-portal files to replace, or is this purely new?
(Answers resolved from repo context: Pages deployment on push to main; no
 existing portal — all greenfield.)

## Synthesized audit object (tdqyq-audit-object)

```text
Target: Generate docs portal via scripts/build-docs-portal.py; validate with
        scripts/check-docs-portal.py; deploy via .github/workflows/pages.yml
Owner/source: scripts/ (new), docs/portal/ (new), .github/workflows/ (new)
Scope: portal generator + CI workflow; no manual HTML edits as canonical source
Non-scope: redesigning existing docs content; changing release artifacts
Acceptance criteria:
  - python scripts/build-docs-portal.py exits 0 and produces dist/docs-portal/index.html
  - python scripts/check-docs-portal.py exits 0 (freshness + nav checks)
  - .github/workflows/pages.yml exists with correct triggers and permissions
Rollback: remove scripts/build-docs-portal.py, scripts/check-docs-portal.py,
          .github/workflows/pages.yml, docs/portal/; revert .gitignore changes
Evidence plan: local build smoke + check script + bash -n workflow syntax check
Authorization: no commit/push/tag/release until separately authorized
```

## Key invariants preserved

- Input gate was not bypassed: empty/unsafe/non-repo inputs would have been
  rejected at this step.
- Smoke A/B, final audit, and authorization gates remain active.
- Natural-language entry did not suppress owner/source discovery or acceptance
  criteria definition.
- IMPLEMENTAUDIT is audit-governed; casual-build intake is a synthesis step,
  not a bypass.
