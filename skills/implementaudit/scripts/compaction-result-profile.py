"""Source-owned profile selection; never supplied by a pending command caller.

P is fixed executable policy before O and remains in assignment code identity.
Later Q/E are immutable external custody data; their arrival never edits P.
The authenticating source-owner implementation/qualification is unresolved.
No source fixture, profile label or H0 receipt supplies that missing owner.
"""
POLICY = {
    'schema': 'implementaudit.compaction-fixed-acquisition-policy.v1',
    'profile_id': 'codex-desktop-0.153.4-collaboration-public-v1',
    'custody_namespace': 'result-observations-v1',
    'qualification_key': 'qualification/Q.json',
    # No independently authenticating source-owner interface is established by
    # the available evidence. This is an explicit missing implementation edge,
    # not a validator returning false and not a caller-selectable adapter.
    'authenticating_source_owner': None,
}
