# Sidecars

Graphify: trigger/anti-trigger; source/version; catalogue/scope/root;
manifest population/file/config SHA-256; graph/scope SHA-256; build;
`llm: false`; result/fallback/live proof.
`--graph-scope <catalog> <repo> <path> [path...]`; parent:
`--graph-parent <catalog> <repo> <scope> <miss\|ambiguity\|cross-scope\|relation-omission>`.

`built_at_commit`; `git rev-parse HEAD`; `--graph-freshness` exit.
query log: scope/nodes/links; orientation only; failure.
Relation omission: fallback; no model.
Privacy/spend: corpus, backend/model, call, cost, filename heuristic;
semantic positive/no-LLM controls.

ActiveGraph: `fork / diff`/mirror; authorised `custody.db`; run-root;
`historical_backfill`.
No sidecar output enters source, commits, or `.skill`.
