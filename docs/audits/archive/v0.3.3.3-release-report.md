# v0.3.3.3 release report

**Status:** corrected same-identity candidate in prepublication qualification;
premature publication preserved as historical evidence; final R29 disposition
and in-place release correction pending

This report is the canonical repository ledger for the `v0.3.3.3` release. It
records the premature publication, the post-publication R29 correction, the
corrected same-identity candidate, and the exact qualification and replacement
cells that remain pending. Historical evidence is append-only; final public
claims apply only after the existing release surfaces are replaced and read
back.

## Release identity and purpose

The public project identity is `v0.3.3.3`. The plugin manifest and installed
runtime identity remain `0.3.3` because the supported host-facing schema uses a
three-component version. The corrective fourth component advances the public
release within that runtime family. It does not rewrite or replace the
`v0.3.3.0` release history.

The release resolves the gap between the public `v0.3.3.0` record and eight
countermeasures integrated afterwards. The four-component public identity is
retained because the runtime remains `0.3.3`. The first `v0.3.3.3` publication
occurred before the authorised campaign reached terminal R29 closure, so owner
authority requires correction of that same identity in place rather than a new
version.

Premature publication commit:
`00591cc4257f1aea1aa9d3d4887ca4069731b074`, tree
`87d12e6920f680fcfa69f65646a1c536240e9528`.

The reviewed PR head `471167975681599e8ea9772d1b6731823b22680f`
produced the same tree as the release commit. The release-identity validators
bound the manifest, runtime skill, changelog, portal metadata, tag family,
canonical ledger basename, commit tree, and release asset to that exact tree.
That qualification remains historical evidence for the superseded package. PR
#173 later merged the corrected R29 owner bytes as commit
`4624ef95542769ecd6a88402fcc585d7c5894c71`, tree
`1f6ae936a7b00bbce86f2a712233655cd776152f`. Final release commit/tree identity
will be recorded only after the composed correction is frozen and qualified.

## Release-body summary

`v0.3.3.3` packages eight interacting countermeasures without changing runtime
version `0.3.3`. IMPLEMENTAUDIT remains reusable meta-engineering for sustained
repository planning, design, implementation, improvement, review, recovery,
integration, and closure. A skill or agent-tooling repository can itself be the
audit object, so the method can govern skills-about-skills. This repository's
self-application is not validation across every agent, host, repository, or
external ecosystem.

- shipped-helper reachability prevents packaged helpers from remaining
  unreachable from the runtime contract;
- scoped Graphify terrain keeps optional code orientation narrow,
  deterministic, and subordinate to live files;
- executor-ready issue work orders turn material findings into bounded work
  another agent can execute;
- engineering-value admission requires each control to protect a named
  consumer and consequence;
- evaluator-integrity protection preserves the original failed witness when a
  test or scoring surface changes after failure;
- governed convergence addresses a repeated same-family rejection without
  imposing the expensive path on ordinary work;
- package semantic preservation keeps runtime and consumer behaviour attached
  while footprint work changes bytes;
- public capability projection checks that release-facing docs neither omit
  material behaviour nor claim more than the evidence supports.

The controls compose as a chain. Reachability exposes the helper. Scoped terrain
limits its orientation input. Work orders preserve executable scope. Value
admission prevents ceremony from becoming its own result. Convergence responds
only to a repeated acceptance family. Evaluator integrity prevents moving the
measurement after failure. Semantic preservation protects package consumers.
Public projection carries the bounded result to README, portal, and release
surfaces.

## Countermeasure record

### R30 / #157: shipped-helper reachability

A helper is not treated as implemented merely because its file ships. The
runtime route and package checks require it to be instruction-reachable,
same-run dispatchable, present in the package, and covered by a negative case.
This is the entry condition for the later Graphify and work-order routes. It is
not evidence that any particular host invoked the helper.

### #144: scoped Graphify terrain

The adopted scope catalogue lives outside the target repository and selects the
smallest qualified graph covering named paths. It binds canonical scan roots,
source population, Graphify configuration, ignore semantics, graph digests,
scope fingerprints, and proper covering parents. Unrepresentable ignore rules,
missing relations, or stale scope state fail closed to exact live-file search.

The qualified route is mechanical `llm: false`. Adoption is deliberately
partial and bounded. Graphify remains a navigation instrument, not a ledger or
correctness proof. The release does not claim Luna execution, semantic model
qualification, inferred repair of extractor omissions, permanent graph
partitioning, or packaged Graphify output.

### R31 / #158: executor-ready issue work orders

Material findings can be expanded into work orders that name owner/source,
scope, ordered steps, acceptance criteria, evidence, rollback, and stop
conditions. Multiple drafts are reconciled before a cold executor receives the
set. Issue creation, body readback, and tracker closure remain separate actions.

The recorded owner disposition is `OWNER_ACCEPTED_PARTIAL`: seven reviewed work
orders are PASS and one is PARTIAL. The partial work order omitted an explicit
R29 invocation. That omission is retained rather than rewritten into an eighth
PASS, and it does not become a claim that all work orders were complete.

### R34 / #163: engineering value and exploratory independence

Every material control must name the consumer it protects and the consequence
it prevents. Review then chooses an evidence-backed lifecycle disposition:
retain, cheapen, merge, condition, retire, or reclassify. This prevents process
volume, control count, or document length from serving as a proxy for
engineering value.

When a material unresolved uncertainty leaves several mechanisms genuinely
plausible and no authoritative discriminator exists, R34 can require
informationally independent first-return hypothesis lanes before synthesis.
This is distinct from independent adversarial review of an already defined
candidate. Bounded, deterministic, single-mechanism, and ordinary coverage work
stay on the cheap path.

### R35 / #164: evaluator and proxy mutation integrity

When a failure is followed by a change to a checker, fixture, threshold, golden,
prompt, or expected answer, the original witness remains part of the evidence.
A second trigger applies when the candidate delta semantically intersects the
validation-shaping authority used to judge that candidate, even without a
post-failure mutation. Candidate-controlled deletion, skip guidance, narrowing,
bypass, or owner unreachability cannot authenticate its own green. A changed
path or product-only delta remains on the ordinary path.

### R32 / #160: governed state-space convergence

The optional convergence route becomes eligible only after a second
independently verified same-family rejection. It records exact primary and
control tuples that bind seed, repository identity, polarity, verdict, semantic
role, and transcript. The validators require Boolean `true` for scoring
properties and reject integer `1`. Ordinary, unrelated, unresolved-family, and
cheap-path work do not enter the route.

R32 consumes R34 exploratory hypothesis discrimination only when family
classification remains materially ambiguous between plausible mechanisms.
Independent verification that a rejection occurred is a separate evidence axis
from informationally independent hypothesis formation. A known family or
defined candidate under adversarial review does not trigger the R34 variant.

This release records deterministic contract coverage only. It does not claim
external model effectiveness or that every repeated failure should use
convergence.

### R33 / #161: package semantic preservation

Footprint work must preserve owner-backed predicates across the runtime,
checkers, installed payload, generated consumers, and public surfaces. Smaller
archive bytes alone do not close a package failure. This control protects the
meaning exposed through reachability and the public projection when packaging
pressure changes representation.

### R29 / #155: public capability projection

Projection activates when all three conditions hold: a material public or
release effect, a declared README or public-doc success carrier, and an intended
current, complete, or release-final claim. It derives public topics from live
owners, records README, docs, and current-state dispositions, and challenges
both omission and overclaiming. Empty identities, malformed types, held-out
mutations, conjunct drops, and Boolean/integer substitutions have deterministic
negative controls.

The prompt-independent model cell has no valid candidate sample. Three
non-samples remain distinct. The first completed process read the wrong globally
installed IMPLEMENTAUDIT skill and is retained as `INVALID` with its unchanged
0/6 score. The next launch ended solely at the controller's one-second outer
timeout before producing a session or scoreable output and remains
`ORCHESTRATOR_TRANSPORT_FAILURE`. The one subsequently authorised call passed
package, fixture, and zero-meter locator preflight, but host tool events showed
zero reads from the task candidate and activation of the unrelated global
`improve` skill; its once-scored 0/6 result is retained as
`INVALID_HOST_RESOLUTION`, not candidate PASS or product FAIL. No retry remains
authorised. The exact installed candidate separately completed a reduced
public-projection dogfood cell that derived the missing `recovery replay` and
`offline archive install` topics, preserved the historical alias, routed
owner-source correction, and refused closure before post-publication readback.
That installed-runtime evidence does not convert any invalid model cell into a
PASS.

After publication, a section-by-section public-owner review found a genuine R29
false pass: the README was materially complete and truthful, but mixed newcomer
orientation with contributor procedure, release chronology, exact campaign
accounting, and deep optional-tool mechanics while `CONTRIBUTING.md` remained
stale. The original acceptance population tested presence, currentness,
omission, and overclaiming but could not distinguish wrong audience, owner, or
abstraction; duplicate current authority; hidden routes; or campaign-dependent
onboarding. PR #173 repaired the source owners and acceptance instrument,
passed hosted validation, merged as
`4624ef95542769ecd6a88402fcc585d7c5894c71`, and reached Pages through run
`31471582620` and deployment `5846669789`. R29 acceptance rows 1–13 and 15–20
are PASS. Row 14 remains `OWNER_DISPOSITION_REQUIRED`: no valid candidate-bound
model sample exists, the three retained attempts remain distinct non-samples,
and no retry is authorised. Final same-tag package and release-body replacement
does not itself resolve that row.

## Exact release package identity

The deterministic archive qualified on the premature publication tree and read
back from the public release has these historical properties:

- asset name: `IMPLEMENTAUDIT.skill`;
- plugin/runtime version: `0.3.3`;
- archive members: 50;
- archive bytes: 227,995;
- SHA-256:
  `bfe323853fcb814530c9f58e078ef09d4e930419d99005af26c9135f936e3536`;
- owner outer bound: 230,000 bytes;
- headroom under that bound: 2,005 bytes;
- `/dashboard/` members: 0.

README, changelog, portal source, contributor guidance, and audit ledgers are
repo-only owners and are not members of the release archive. The R29
countermeasure changes packaged runtime guidance, so the corrected same-identity
candidate is 227,989 bytes with SHA-256
`d2d7138b7e74b23e27ef3f10bfde05d94bc81d92ef7e80be1f1e22fd081494b5`.
It has 50 members, zero dashboard members, and 2,011 bytes of headroom under the
unchanged 230,000-byte outer bound. Focused package, parity, temporary-install,
and ordinary-run checks passed against those exact bytes. The existing public
asset remains the superseded 227,995-byte payload until final qualification and
authorised in-place replacement of `IMPLEMENTAUDIT.skill` and `CHECKSUMS.txt`
on release ID `368332810`.

## Premature publication custody

The superseded publication remains reconstructible:

- annotated tag object
  `89bf563c965e65e189f6bfa9ab5008f946f3081e`, created
  `2026-08-11T04:31:23Z`, dereferences to commit `00591cc...` and tree
  `87d12e...`;
- GitHub release ID `368332810`, created `2026-08-11T04:31:23Z`, published
  `2026-08-11T04:31:53Z`, and last updated `2026-08-11T04:33:48Z` before this
  correction;
- `IMPLEMENTAUDIT.skill` asset ID `509660798`, created and updated
  `2026-08-11T04:31:53Z`, 227,995 bytes, SHA-256
  `bfe323853fcb814530c9f58e078ef09d4e930419d99005af26c9135f936e3536`;
- `CHECKSUMS.txt` asset ID `509660797`, created and updated
  `2026-08-11T04:31:53Z`, 96 bytes, SHA-256
  `bb8c63c1f8b45354c96e5bbe04953767e85c036db6def5f8e0d98fa85da5c081`;
- the insider-facing release body, 14,532 UTF-8 bytes and 275 lines, SHA-256
  `21e7e7b379dbf9a312643186e749d4fb5bd97cdbdde3ad162e3f42f77da6751e`;
- the post-publication R29 false-pass witness and PR #173 correction described
  above.

Replacement will preserve these identities as history while making the current
release object describe only the final corrected `v0.3.3.3` state. New asset
IDs, tag object/target, checksum identity, release-body digest, and replacement
timestamps remain blank until independent post-mutation readback.

## Dispositions and exclusions

- R31 remains `OWNER_ACCEPTED_PARTIAL`, with 7 PASS and 1 PARTIAL. The partial
  is the missing explicit R29 invocation.
- #144 remains bounded deterministic partial adoption with `llm: false`. No
  Luna or semantic qualification is claimed.
- R36 / #167 remains open. It was not shipped or qualified in this release and
  will re-anchor from exact post-release main under its own implementation
  authority.
- R28 / #117 remains open, optional, owner-paused, and nonblocking.
- `/dashboard/` remains excluded from `IMPLEMENTAUDIT.skill`.
- No marketplace publication, signature, attestation, SBOM, licence,
  provenance, universal-host support, passive update, or host-load claim is
  made.

## Qualification and publication cells

### Historical exact qualification

- **Release identity:** commit
  `00591cc4257f1aea1aa9d3d4887ca4069731b074`, tree
  `87d12e6920f680fcfa69f65646a1c536240e9528`.
- **Reviewed identity:** PR head
  `471167975681599e8ea9772d1b6731823b22680f` has the same tree as the release
  commit; the full tree diff is empty.
- **Complete verifier:** the single complete verifier on the exact release
  source returned exit zero with `verify-package: ok`. It included all
  registered deterministic tests, R29 32/32, R30 18/18 plus 3/3 modes, the R31
  structural model-cell contract without a model call, R32, R33, R34 71/71,
  R35 39/39, docs 37/37, validation registry 69, reproducibility, package, and
  install tests.
- **Package identity:** 227,995 bytes, 50 members, zero dashboard members,
  SHA-256
  `bfe323853fcb814530c9f58e078ef09d4e930419d99005af26c9135f936e3536`;
  2,005 bytes of headroom under the 230,000-byte owner bound.
- **Independent review:** fresh exact-tree review accepted the source tree,
  package identity, release notes, publication plan, R31 partial disposition,
  #144 deterministic boundary, R36 exclusion, and #117 boundary for
  publication.

### Corrected candidate qualification

- **Source owner state:** PR #173 merged as commit
  `4624ef95542769ecd6a88402fcc585d7c5894c71`, tree
  `1f6ae936a7b00bbce86f2a712233655cd776152f`. Hosted validation run
  `31471582591`, Pages run `31471582620`, deployment `5846669789`, and public
  homepage readback completed against that exact commit.
- **Candidate package:** 227,989 bytes, 50 members, zero dashboard members,
  SHA-256
  `d2d7138b7e74b23e27ef3f10bfde05d94bc81d92ef7e80be1f1e22fd081494b5`;
  2,011 bytes of headroom under the 230,000-byte owner bound.
- **Focused evidence:** R29 33/33, R30 18/18 plus 3/3 modes, R33 17/17, R35
  39/39, release-asset/package checks, source/package parity, temporary Codex
  install, archive/runtime/installed parity, installed R29 instruction
  presence, ordinary-run helper execution, and exact-tree diff check passed.
- **Complete verifier:** **PENDING TERMINAL EVIDENCE**. One complete verifier
  invocation on the post-PR #173 tree timed out at the controller boundary and
  left no recoverable terminal result. Focused PASS results do not substitute
  for this final composed gate, and no duplicate broad run was started merely
  for activity.
- **Exact final identity:** **PENDING**. This repo-only public-owner change does
  not alter packaged members, but final commit/tree, same-tag target, fresh
  exact-tree review, hosted validation, and Pages readback bind only after the
  composed tree is frozen.

### Historical public readbacks

- **Tag:** annotated tag object
  `89bf563c965e65e189f6bfa9ab5008f946f3081e` dereferences to release commit
  `00591cc4257f1aea1aa9d3d4887ca4069731b074`.
- **GitHub release:** public release ID `368332810`, title
  `v0.3.3.3 — Corrective completion of the v0.3.3 runtime family`, with draft
  and prerelease both false.
- **Assets:** release asset `IMPLEMENTAUDIT.skill`, asset ID `509660798`, is
  227,995 bytes with API and fresh-download SHA-256
  `bfe323853fcb814530c9f58e078ef09d4e930419d99005af26c9135f936e3536`.
  Release asset `CHECKSUMS.txt`, asset ID `509660797`, is 96 bytes with SHA-256
  `bb8c63c1f8b45354c96e5bbe04953767e85c036db6def5f8e0d98fa85da5c081`.
  The downloaded checksum manifest validates the downloaded skill asset.
- **Hosted validation:** exact-release-main validation run `31456284912`
  completed successfully.
- **Pages:** exact-release-main Pages run `31456284913` and deployment
  `5843992982` completed successfully. All 32 generated routes returned HTTP
  200 and matched the locally generated exact-head output after newline
  normalisation.
- **Public install:** a fresh public download installed into a temporary Codex
  home with 48/48 non-plugin payload parity. The installed `detect-env` route
  returned exit zero. This is copy/install evidence, not passive update or
  universal host-load proof.

These readbacks establish the superseded publication only. They must not be
credited to the corrected candidate or its pending replacement assets.

### Remaining post-publication cells

- **Tracker:** **PENDING ROW 14 OWNER DISPOSITION**. Rows 1–13 and 15–20 are
  PASS. #155 cannot terminalise until row 14 receives an explicit owner waiver
  or deferral, or a separately authorised candidate-bound execution occurs.
  R36/#167 and R28/#117 remain open under their own contracts.
- **Repository-owner transition:** **PARTIAL**. PR #173 merged the packaged R29,
  README, CONTRIBUTING, and diagram/public-owner correction and reached Pages
  at exact merge commit `4624ef...`. This same-tag release-history and portal
  reconciliation still needs review, merge, hosted validation, Pages
  deployment, and public-source readback before it becomes current public
  source.
- **R29 comprehension:** **OWNER DECISION REQUIRED**. The authorised call used
  the unchanged prompt, evaluator, expected answers, model, and corrected
  candidate bytes, but host resolution activated the unrelated global
  `improve` skill. Session `019fefbc-b175-77f3-92fb-4b1f92245b62` read zero
  candidate files; the 0/6 scorer output is `INVALID_HOST_RESOLUTION`, not a
  candidate outcome. Its result receipt hashes to
  `c97a89b1860b7ad8c62c73e0108fa5db663ed01f91a450bc5184584608d5173d`.
  No further retry is authorised, and #155 must remain open unless its owner
  supplies a disposition or authorises a new candidate-bound execution route.
- **Final in-place publication:** **PENDING**. Freeze and qualify one exact final
  commit/tree, align the existing `v0.3.3.3` tag as the repository contract
  requires, replace the two release assets and user-facing body on release ID
  `368332810`, then independently read back tag/tree, asset IDs and bytes,
  checksum validation, hosted validation, Pages, public download, temporary
  install, and installed runtime `0.3.3`.
- **Installed-runtime projection:** the historical install receipt covers only
  superseded bytes. Replacement public bytes need their own copy, parity, and
  ordinary-run smoke. No passive update is assumed.

## Post-publication Hansei

The public-information-architecture incident has two causal layers. First,
README accumulated true material whenever a public checker needed a reachable
literal, while the changelog, release report, contributor guide, and portal
already had more specific consumers. Ownership was therefore judged by truth
and presence, not by audience job or useful abstraction. Second, R29 inherited
that same state model: its census could prove that every topic existed and was
current without proving that one discoverable authority served the intended
reader or that duplicate/current placements were explanatory rather than
competing.

The countermeasure extends the existing R29 projection record and cold-review
route; it does not create a documentation lifecycle or rigid README template.
Equivalent cases—chronology dumped into onboarding, maintainer instructions
displaced from contributor guidance, duplicate release tables, campaign jargon
hiding current capability, or stale contributor docs—must now fail the shared
audience/authority/abstraction property. Substantial offline README content,
concise delegation, multi-purpose small-repo owners, and private cheap paths
remain valid.

A separate publication-sequencing lesson also applies: before an irreversible
release mutation, mechanically reconcile the complete live issue-level
acceptance population with the exact release ledger. A known `NOT_RUN` row must
not survive merely because the release-level verifier is green. This lesson is
recorded here as a bounded Hansei disposition; it does not by itself create a
new issue, worksheet, or gate.

## Release boundary

The release object is public, but its present tag target, assets, checksum, and
body are a premature publication, not terminal `v0.3.3.3` campaign closure.
Editing this report does not establish final source merge, tracker disposition,
exact-tree qualification, tag correction, asset/body replacement, hosted or
Pages success, or public download/install readback. Each requires independent
post-mutation evidence. A successful local docs build proves generated-site
coherence only.

The `v0.3.3.0` release report remains unchanged as historical evidence. This
corrective report retains the distinct `v0.3.3.3` identity and does not alter
`v0.3.3.0` claims, assets, or tracker accounting. The final authorised topology
is correction of the existing `v0.3.3.3` publication in place, with premature
identities preserved internally and replacement state read back before closure.
