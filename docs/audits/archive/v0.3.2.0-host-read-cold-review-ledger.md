# v0.3.2.0 host-read cold-review ledger

Status: `APPEND-ONLY REJECTION EVIDENCE`

Runtime/model identity: Sol. Every independent review was read-only and
performed by Sol, not Fable.

## Review 1

- Reviewed commit: `c06517723a1cbfc805259bd4c9b47de877143b7b`
- Verdict: `BLOCK`
- Focused host suite: PASS
- Full package: PASS, 59 tests in both registries, 374.6 seconds
- Worktree: clean; no push or integration

Blocking findings:

1. Any Codex `/bin/bash -lc` text was treated as a host-owned wrapper without
   trusted wrapper provenance.
2. Codex start/completion correlation bound only item type, not the complete
   action payload.
3. Scoped search output shaping (`--no-filename`, `--replace`) could forge
   filename provenance.
4. Shell/executable attestation was declarative and unused.
5. Dynamic variable paths lost the `fail-closed` distinction.

Disposition: H46 RED controls were added before the second implementation.

## Review 2

- Reviewed commit: `245feb799ca13f05a0648404ba787c1407c4c1d2`
- Verdict: `BLOCK`
- Focused host suite: PASS
- Implementer package gate: PASS, 59 tests in both registries, 409.7 seconds
- Worktree: clean; no push or integration

Blocking findings:

1. The supposedly trusted profile was constructor-supplied and test-only; no
   production adapter path minted or persisted it, and real Codex records were
   never marked as the single outer protocol wrapper.
2. Arbitrary profile IDs and executable strings could manufacture positive
   evidence without PATH/resolution/hash verification.
3. Completion-only Codex actions invented invocation ordinals and could
   false-pass read-before-write ordering.
4. `head -c 0` still manufactured a content read.
5. Valid Claude Glob/Grep actions were not correlated; their results became
   unmatched invalid records.
6. Cross-dialect and long-option behavior remained under-specified.

The review additionally showed that exact-code replay of retained B3-v3 raw
evidence does not reproduce its recorded 12/12 host result.

## Convergence mapping

| Repeated rejection | Convergence state family |
|---|---|
| wrapper/profile provenance | formal Codex trust and replay custody |
| invocation/completion identity | global typed action intervals |
| Claude allowed-tool handling | Claude tool normalization |
| option/dialect/output semantics | finite shell/reader semantics |
| stored result not reconstructible | two-phase custody and replay |
| process access versus model receipt | product-semantic read contract |

These rejected commits and passing package gates are preserved as evidence that
the previous deterministic registries were incomplete. They are not accepted
qualification commits and must not be integrated.

## Review 3

- Reviewed commit: `79cc2fe304ad4397bc65237435f4a9618cf5edf3`
- Verdict: `BLOCK`
- Focused contract: PASS, 112/112
- Focused host suite: PASS, H1-H48
- Full package: PASS, 59 tests in both registries, 421.1 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Worktree: clean; no push or integration

Blocking findings:

1. Formal Codex normalization accepted bare reader commands with zero outer
   wrapper layers. Paired bare `cat` reads followed by a native write could
   produce host, property, and overall PASS although formal evidence requires
   exactly one attested `/bin/bash -lc` protocol envelope.
2. The shell tokenizer disabled comment parsing. A target named only after an
   unquoted `#` comment marker could be treated as a reader operand; decoy
   output byte-identical to the target preimage then manufactured content-read.
3. A caller-created dictionary could self-assert
   `authority=mechanically-minted` with nonexistent shell/reader identities and
   pass formal profile validation.
4. Snapshot path identity ignored `repo.case_sensitive=false`, so a valid
   case-variant spelling on a case-insensitive repository failed closed.

Disposition: add the four exact reviewer probes as deterministic RED controls;
repair formal profile admission, exact wrapper enforcement, comment handling,
and snapshot-relative case semantics coherently; rerun focused and full gates;
then request a fourth independent exact-commit cold review.

## Review 4

- Reviewed commit: `7b9a0e7bf5c4d52f7d16a1c0d93669fdafc81b77`
- Verdict: `BLOCK`
- Independent review mode: read-only targeted probe
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48
- Implementer full package: PASS, 59 tests in both registries, 430.3 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Worktree at review start: clean; no push or integration

The independent reviewer found a release-blocking false-green before its final
report was interrupted by an automated content filter. The main Sol executor
then reproduced the exact probe against the reviewed commit:

1. With snapshot `case_sensitive=false`, a completed native write to a
   case-variant spelling of S before a later valid S read was not recognized as
   the same file. The adjudicator returned `live_preimage=true`,
   `property_status=PASS`, and `overall_status=PASS`.
2. The same missing case-aware equality rule made a valid Claude Read use and
   result pair INVALID when the structured result used a case-variant spelling
   of the same file.

The executor's finite audit also retained two adjacent states from the original
adversarial sweep before editing production code:

3. Claude relative tool-use paths and root-bound absolute result paths were not
   treated as the same lexical repository identity.
4. A mechanically minted formal profile was mutable at the top level and
   recursively through its nested repository and requested-tool structures.
5. Codex native-session `cwd` corroboration used raw case-sensitive equality
   even when the profiled repository declared `case_sensitive=false`.

Disposition: H47p-H47s were observed RED together, then H47t was independently
observed RED during the exhaustive identity-site audit. Repair the shared path
identity invariant and recursively freeze minted profiles; rerun focused and
full gates; then obtain a new independent exact-commit cold review. The
interrupted review runner's package-gate outcome is not claimed.

## Review 5

- Reviewed commit: `a709dd572d317a4a451266e04dfec6ad49c8a18c`
- Verdict: `BLOCK`
- Focused contract: PASS, 112/112
- Focused host suite: PASS, H1-H48 including H47p-H47t
- Full package: PASS, 59 tests in both registries, 423.208 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. Nested frozen profile containers could still be mutated through their bound
   `__init__` methods; both repository metadata and the requested-tool list
   changed canonical bytes while formal validation continued to pass.
2. Formal normalizers did not bind profiles to the expected host. Claude
   accepted a missing or Codex profile; Codex raised `KeyError` instead of
   failing closed for a Claude profile.
3. Profileless non-formal correlation of matching absolute paths regressed to
   INVALID, contradicting the retained lexical-only compatibility boundary.
4. A Claude result containing contradictory top-level and nested file paths
   completed because only the top-level representation was inspected.
5. Stored `probe_sha256` values were shape-checked but not recomputed from the
   admitted profile fields.

Disposition: add exact H47u-H47y RED controls; make frozen construction
create-only, recompute profile digests, bind formal profiles to their host,
separate formal root-bound path resolution from non-formal lexical
correlation, and require every present Claude result path to agree. Rerun all
gates and request another exact-commit cold review.

The executor's pre-commit admission audit added two related controls rather
than leaving the same trust boundary implicit: H47z binds Claude's exact
requested-tool list to the minted profile, and H47aa binds the profile and
preimage snapshot to the same repository. Both were separately observed RED
before their production changes and are part of the sixth-review gate.

## Review 6

- Reviewed commit: `87c1c983286b85d5139b73729f62b36e7ff24377`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47u-H47aa
- Implementer full package: PASS, 59 tests in both registries, 433.7 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. Formal Claude normalization recorded a malformed non-mapping profile as an
   invalid finding but continued into path handling that assumed the profile
   was a mapping. A non-empty list could therefore raise `AttributeError`
   instead of returning host `INVALID`.
2. Preimage validation required the target key to equal `relative_path` and
   required `canonical_path` to remain beneath the repository root, but did
   not require `canonical_path` to identify that same relative file. A snapshot
   could bind `A.txt` bytes to the canonical identity of in-repository
   `B.txt`, pass formal capture admission, and later adjudicate against the
   unrelated canonical identity.

Disposition: add H47ab-H47ac as deterministic RED controls; make every
profile consumer total over malformed inputs and require each preimage's
canonical path to equal its repository root plus relative path under the
recorded case semantics. Rerun focused and full gates, then request a seventh
independent exact-commit cold review.

## Review 7

- Reviewed commit: `155455152c70d70b8f379aa3600a1083d6b97bf2`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47ab-H47ac
- Implementer full package: PASS, 59 tests in both registries, 409.8 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suite: PASS
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. Claude requested-tool comparison still converted the nested
   `native_tools.requested` value with `list(...)` before proving it iterable.
   Integer and Boolean values raised `TypeError` in both normalization and the
   replay-boundary helper instead of failing closed.
2. Direct Codex session corroboration consumed `repo.case_sensitive` without
   requiring an exact Boolean. Otherwise-valid lineage could return `VALID`
   with malformed string, list, integer, null, or mapping values. Formal replay
   validates earlier, so no persisted-capture bypass was demonstrated, but the
   shared helper contract remained non-total.
3. `_within()` stripped the POSIX filesystem root `/` to an empty string. A
   valid root-repository identity such as `/etc/hosts` was therefore rejected;
   ordinary POSIX roots, Windows drive/UNC roots, spaces, Unicode, separator
   normalization, and case-insensitive equivalents remained accepted.

Disposition: add H47ad-H47af as deterministic RED controls; type-check the
nested requested-tool list before comparison, require exact-Boolean filesystem
semantics at session admission, and preserve POSIX root identity in bounded
path containment. Rerun focused and full gates, then request an eighth
independent exact-commit cold review.

## Review 8

- Reviewed commit: `5343e116ce364095e815d8a8737a07c3c126df60`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47ad-H47af
- Implementer full package: PASS, 59 tests in both registries, 380.1 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suite: PASS
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. Both formal normalizers converted only falsey bindings to `{}`. A non-empty
   list, string, integer, or Boolean then reached `.get` and raised
   `AttributeError` instead of returning host `INVALID`. Persisted replay also
   consumed terminal binding fields as a mapping before proving their shape.
2. Root trimming collapsed absolute Windows drive root `C:/` to drive-relative
   `C:`. Containment and equality therefore treated `C:` as equal to and within
   `C:/`, weakening the absolute-root boundary.
3. Formal profile validation rejected empty requested-tool names, while replay
   validation and profile/replay matching accepted `requested_tools=[""]`.

Disposition: add H47ag-H47ai as deterministic RED controls spanning
normalization, capture sealing, session corroboration, replay, drive-root
identity, and non-empty tool-name admission. Repair these shared boundaries as
one tranche, rerun focused and full gates, then request a ninth independent
exact-commit cold review.

## Review 9

- Reviewed commit: `97b3fca471dc82fe48fcd322af55fa98d87a877d`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47ag-H47ai
- Implementer full package: PASS, 59 tests in both registries, 429.7 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suite: PASS
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. Drive-relative `C:` was still accepted as a containment root, and Unicode
   `casefold()` merged Windows path identities such as `Straße` and `STRASSE`
   even though the recorded filesystem semantics do not.
2. Codex replay admission and profile matching accepted a non-empty
   `requested_tools` list even though Codex has no corresponding requested-tool
   authority surface.
3. Binding validation was structural rather than host-specific. A Codex
   binding could therefore include a foreign Claude `session_id` (and vice
   versa) without admission failing.
4. Session corroboration treated an unknown host as Claude and converted
   malformed raw-session values before its guarded parse, so it could either
   return a false `VALID` or raise instead of failing closed.
5. Capture sealing validated `trace.observed_tools` only after writing raw and
   derivative files. A malformed integer could raise after a partial terminal
   capture had already been materialized.

Disposition: add H47aj-H47an as deterministic RED controls; align path identity
with recorded host semantics, bind requested tools and lineage schemas to the
declared host, make session corroboration total, and validate every terminal
capture input before the first write. Rerun focused and full gates, then
request a tenth independent exact-commit cold review.

## Review 10

- Reviewed commit: `ebba9707123118f620969c9627259145c32b279b`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47aj-H47an
- Implementer full package: PASS, 59 tests in both registries, 441.5 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suite: PASS
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. A truthy non-string action ID was retained in the invalid action record.
   Trace finalization then called `.startswith()` on that Boolean or number,
   raising instead of returning host `INVALID` in both normalizers. Because
   normalization precedes terminal sealing, this could erase product-property
   evidence for the mission.
2. Codex native-session objects were syntactically JSON mappings, but their
   `session_meta.payload` or `turn_context.payload` values were not required to
   be mappings. Lineage augmentation and corroboration called `.get()` on an
   integer and raised before terminal evidence could be sealed.

Disposition: add H47ao-H47ap as deterministic RED controls; give every invalid
host event an internal string identity, validate Codex native payload mappings
before field access, and prove an invalid trace still seals reconstructible
property evidence. Rerun focused and full gates, then request an eleventh
independent exact-commit cold review.

## Review 11

- Reviewed commit: `b5d39cf0cd0ec09e2fde13d92a6957dfb0010da3`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47ao-H47ap
- Implementer full package: PASS, 59 tests in both registries, 413.3 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suite: PASS
- Reviewer 40-case malformed-action probe: PASS without exceptions
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. H47ao2 sealed an invalid trace and incomplete property matrix but did not
   create parent custody or call the official replay path. Formal replay raised
   on session `INVALID` before regenerating derivatives, returning only a
   status and erasing official access to the stored property measurements.
2. The pure Codex lineage helpers were total, but the production adapter's
   session selection still called `.get()` on untyped top-level JSON and on
   non-mapping `session_meta`/`turn_context` payloads. Multiple retained shapes
   raised `AttributeError` before formal capture could complete.
3. Claude normalization accepted an observed inventory containing an empty
   tool name as host PASS, while terminal trace validation rejected it. The
   resulting disagreement prevented malformed host evidence from sealing with
   preserved property results.

Disposition: strengthen H47ao2 through formal parent custody and replay,
strengthen H47ap through the production adapter, and add H47aq for malformed
observed inventories. Replay must distinguish reconstructible custody from host
safety so an INVALID host layer cannot erase property measurements. Rerun all
gates, then request a twelfth independent exact-commit cold review.

## Review 12

- Reviewed commit: `486d80c5f46caf3f37912509b01a71604a04209c`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract: PASS, 112/112
- Implementer focused host suite: PASS, H1-H48 including H47ao2/H47aq2
- Implementer full package: PASS, 59 tests in both registries, 419.4 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suite: PASS
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking findings:

1. The formal host-read branch still returned `HostRunResult("invalid", ...)`
   before ordinary host checks, repository after-state, bundle construction,
   and official scoring. H47ao2/H47aq2 proved capture-level replay only. For
   B3-v3, that preserved the path-order measurement but erased the other five
   declared properties from the official verdict path.
2. Formal replay used `terminal.get(...)` before requiring a mapping and
   admitted missing or unknown `host_terminal_kind` values as though they were
   `ok`. A rehashed terminal array raised `AttributeError`; a rehashed
   `host_terminal_kind="forged"` replayed as host PASS. A truthy non-mapping
   `post_probe.shell` also raised during profile validation.
3. Production Codex session selection and reading remained non-total over
   malformed inner fields and invalid UTF-8. Non-string `payload.cwd`,
   unhashable session identities, and invalid encoding could raise before the
   formal evidence path completed.

Disposition: add end-to-end official-verdict coverage proving all six B3-v3
property states survive a separately recorded host INVALID; add strict terminal
mapping/schema/type/enum admission and post-probe totality; add production
Codex inner-field and invalid-encoding controls. Preserve the parent-terminal
custody gate: host invalidity must enter through a separately hash-bound host
safety record rather than by making a non-authoritative parent terminal score.
Rerun focused and full gates, then request a thirteenth independent exact-commit
cold review.

## Review 13

- Reviewed commit: `59942130e2b941030af5699bb1545cd84449815f`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused contract/host suites: PASS, including H47ar-H47au
- Implementer full package: PASS, 59 tests in both registries, 448.3 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused host suite and compilation/diff checks: PASS
- Reviewer full package: not rerun after the exact commit was already blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking finding:

The tagged scorer independently replayed and cross-checked only the
`path_access_order` property. The four B3-v3 `json_fields_equal` results still
fell through to adapter-produced Booleans. Repository snapshots retained the
capsule hash but the bundle did not retain its bytes, so the scorer could not
reconstruct those field comparisons. A read-only counterexample used capsule
bytes `{}` with a simulated defective adapter returning `true` for the four
field checks; official scoring accepted all six properties as PASS despite the
bound after-snapshot correctly hashing `{}`.

Disposition: add H47av as a permanent negative control. Extend repository
snapshots with a complete regular-file identity map, retain each declared JSON
host-check input as a hash-bound bundle artifact, bind those bytes to the
after-snapshot file hash, independently recompute each fixture-declared field
comparison, and reject any disagreement with `host-checks.json`. No adapter
Boolean may manufacture a JSON-property PASS.

## Review 14

- Reviewed commit: `fd693723ddbcb19754387ff8b2fb2816f7f06260`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused host suite: PASS, including H47av and 112/112 contract
- Implementer full package: PASS, 59 tests in both registries, 397.9 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer full package: not rerun after the exact commit was blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking finding:

Review 13's adapter-Boolean false accept was closed: the tagged scorer now
reconstructed every B3-v3 JSON-field result from retained bytes bound to the
after-snapshot and rejected the `{}` counterexample. The new complete worktree
map, however, excluded `.git` only when it was a directory. A linked Git
worktree exposes `.git` as an administrative pointer file, so the snapshot
hashed that pointer as ordinary repository evidence. This violated the
explicit Git-metadata exclusion boundary and made otherwise identical source
snapshots depend on checkout administration.

The reviewer also confirmed that a bound but undeclared
`host-check-inputs/unreferenced.json` artifact was accepted. It could not
manufacture a property result and was therefore classified as non-blocking
hardening, but the reserved evidence namespace remained ambiguous.

Disposition: add H47aw-H47ax as deterministic RED controls. Exclude the root
`.git` identity whether it is a directory or linked-worktree pointer file, and
require every artifact in the reserved `host-check-inputs/` namespace to match
a fixture-declared regular-file input. Rerun focused and full gates, then
request a fifteenth independent exact-commit cold review.

## Review 15

- Reviewed commit: `cd773a086c486fda737967a3d2d7084de16f8a3b`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused host/evaluator suites: PASS, including H47aw-H47ax
- Implementer full package: PASS, 59 tests in both registries, 389.4 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer focused contract/host suites: PASS
- Reviewer full package: not rerun after the exact commit was blocked
- Final HEAD/worktree: exact and clean; no push or integration

Blocking finding:

Replay-time snapshot validation admitted self-consistently rehashed
`worktree_files` entries with missing digests, unknown byte-bearing fields,
slash/backslash aliases, absolute/dot/empty/NUL path identities, and root Git
administrative identities. An official E5 bundle carrying the malformed map
scored product PASS, host PASS, and overall PASS. Capture-time exclusion was
therefore insufficient: a bound-but-malformed replay snapshot could cross the
same evidence boundary.

Disposition: add H47ay as an official-scorer negative control over every
malformed class. Require canonical relative POSIX identities, reject drive/UNC,
dot, empty, NUL, backslash, and root `.git` identities, and require exact
digest-only entry schemas: file `{type, sha256}`, symlink
`{type, target_sha256}`, special `{type}`. Rerun focused and full gates, then
request a sixteenth independent exact-commit cold review.

## Review 16

- Reviewed commit: `47ccdc641d2122b1d0a941f7d1bb991b24e1a680`
- Verdict: `BLOCK`
- Independent review mode: read-only
- Implementer focused evaluator suites: PASS, including H47ay and 112/112
  host-read contract states
- Implementer full package: PASS, 59 tests in both registries, 334 seconds
- Reproducible intermediate asset SHA-256:
  `317ffbd34ed5387a0a00ee99f75d1d46ac8fbd87ba8f7be2a46414c944593d84`
- Reviewer adversarial suite: PASS, including H47ay
- Reviewer bounded validator matrix: PASS for 22 direct schema/path cases
- Final HEAD/worktree: exact and clean; no push or integration

Review-runner note: the first fresh-context attempt stopped at an automated
content filter and produced no technical disposition. It was classified as
`transport-infrastructure` and superseded by a narrower read-only retry. No
repository or GitHub state changed during either review attempt.

Blocking finding:

Capture excludes the root `.git` administrative identity case-insensitively on
Windows, but replay compared the first path component to `.git`
case-sensitively. Self-consistently rehashed official bundles using
`.GIT/config` or `.Git/config` therefore scored product PASS, host PASS, and
overall PASS even though those identities alias Git administration on the
capture platform. H47ay covered only lowercase `.git` spellings.

Disposition: add H47az as an official-scorer negative control for root Git
administrative case aliases. Reject the first path component case-insensitively
at replay, independent of the replay host's filesystem semantics. Rerun focused
and full gates, then request a seventeenth independent exact-commit cold review.
