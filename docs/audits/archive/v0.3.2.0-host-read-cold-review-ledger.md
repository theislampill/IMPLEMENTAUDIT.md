# v0.3.2.0 host-read cold-review ledger

Status: `APPEND-ONLY REJECTION EVIDENCE`

Runtime/model identity: Sol. Both reviews were independent, read-only, and
performed by Sol, not Fable.

## Review 1

- Reviewed commit: `c06517723a1cbfc805259bd4c9b47de877143b7b`
- Verdict: `BLOCK`
- Focused host suite: PASS
- Full package: PASS, 59 tests in both registries, 374.6 seconds
- Worktree: clean; no push or integration

Blocking findings:

1. Any Codex `/bin/bash -lc` text was treated as a host-owned wrapper without
   trusted wrapper provenance.
2. Codex start/completion correlation bound only item type, not the complete
   action payload.
3. Scoped search output shaping (`--no-filename`, `--replace`) could forge
   filename provenance.
4. Shell/executable attestation was declarative and unused.
5. Dynamic variable paths lost the `fail-closed` distinction.

Disposition: H46 RED controls were added before the second implementation.

## Review 2

- Reviewed commit: `245feb799ca13f05a0648404ba787c1407c4c1d2`
- Verdict: `BLOCK`
- Focused host suite: PASS
- Implementer package gate: PASS, 59 tests in both registries, 409.7 seconds
- Worktree: clean; no push or integration

Blocking findings:

1. The supposedly trusted profile was constructor-supplied and test-only; no
   production adapter path minted or persisted it, and real Codex records were
   never marked as the single outer protocol wrapper.
2. Arbitrary profile IDs and executable strings could manufacture positive
   evidence without PATH/resolution/hash verification.
3. Completion-only Codex actions invented invocation ordinals and could
   false-pass read-before-write ordering.
4. `head -c 0` still manufactured a content read.
5. Valid Claude Glob/Grep actions were not correlated; their results became
   unmatched invalid records.
6. Cross-dialect and long-option behavior remained under-specified.

The review additionally showed that exact-code replay of retained B3-v3 raw
evidence does not reproduce its recorded 12/12 host result.

## Convergence mapping

| Repeated rejection | Convergence state family |
|---|---|
| wrapper/profile provenance | formal Codex trust and replay custody |
| invocation/completion identity | global typed action intervals |
| Claude allowed-tool handling | Claude tool normalization |
| option/dialect/output semantics | finite shell/reader semantics |
| stored result not reconstructible | two-phase custody and replay |
| process access versus model receipt | product-semantic read contract |

These rejected commits and passing package gates are preserved as evidence that
the previous deterministic registries were incomplete. They are not accepted
qualification commits and must not be integrated.
