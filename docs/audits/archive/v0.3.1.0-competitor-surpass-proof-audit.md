# v0.3.1.0 Competitor-Surpass Proof Audit

Status: source milestone proof ledger.

Scope: assess all supplied attachments for the v0.3.1.0 source milestone:
the initial user request, the later correction that prior closure only repaired
ignored local run-root evidence, Claude's findings text, `improve-main(1).zip`,
and `supergoal-main(3).zip`.

Boundaries preserved: No commit. No push. No tag. No release. No publication.
No provenance. No issue creation. No real-home install. No license selection.
No comparator command identity was imported into the runtime.

open PR/fork/donor branch: none. The authoritative comparator inputs were the
two zip files supplied by the user, not an open upstream PR or fork branch.

## Verdict

Final verdict: V0_3_1_0_PROVEN_WITH_WEAKNESSES

Reason: the comparator matrices have zero `FAIL` and zero `UNVERIFIED` rows,
and the deterministic/package checker surface passes. The local Codex CLI
self-dogfood lane was proven for the pre-RC2 payload through a temp-only
authenticated Codex home and a narrow temp-only runner-policy profile. The
authenticated `codex exec` process launched from the IMPLEMENTAUDIT repo root
using the temp-installed local skill, preserved baseline-first behavior, avoided
installed-payload readback before baseline, executed the required safe
repo-local validation commands, and emitted `AUDIT_COMPLETE` before
`IMPLEMENTAUDIT_RUN_COMPLETE`. RC2 changes installed payload files after that
transcript, so RC2 dogfood status is
`CODEX_SELF_DOGFOOD_NOT_RERUN_AFTER_RC2` unless a future temp-policy Codex CLI
run is performed. The earlier
`CODEX_CLI_SELF_DOGFOOD_BLOCKED_BY_LOCAL_EXEC_POLICY` classification is retained
as historical root-cause evidence for the default-rules lane and is superseded
for the pre-RC2 payload by
`CODEX_CLI_POLICY_REPAIRED_AND_SELF_DOGFOOD_PROVEN`. This verdict remains
weaker than full proof because public issue creation is explicitly deferred,
license selection remains an owner decision, RC2-specific CLI dogfood has not
been rerun, and no Codex app/web/cloud dogfood lane is claimed. This verdict
does not claim a GitHub release, marketplace publication, provenance,
real-home install, public issue creation, or selected license.

## RC3 Andon: stale real-home installed skill contamination

| Field | Evidence |
|---|---|
| Classification | `ANDON_PROBE`; class `stale-installed-skill / real-home-contamination`. |
| Abnormality | The RC3 proof attempt loaded `C:\Users\theis\.codex\skills\implementaudit\SKILL.md` and chunk-read the stale real-home installed IMPLEMENTAUDIT skill before building or installing the RC3 temp-home artifact. |
| Impact | This readback is NON-EVIDENCE for RC3 dogfood because it tests the active real-home install surface, not the fresh RC3 `.skill` installed into a temporary `CODEX_HOME`. |
| Dogfood status | No RC3 dogfood verdict is claimed from this contaminated attempt. The earlier local validation log remains local deterministic validation only. |
| Countermeasure | Strengthen `scripts/check-dogfood-bootstrap-contract.sh` and `tests/dogfood-bootstrap-contract.test.sh` with a tracked negative transcript fixture that fails when `C:\Users\theis\.codex\skills\implementaudit\` appears before a temp `CODEX_HOME` / temp installed skill path is established. |
| Required fresh proof fields | Future RC3 dogfood must record temp `CODEX_HOME`, installed skill path under that temp home, installed `SKILL.md` line/byte count, exact Codex command proving that temp home was used, transcript/output path, final marker evidence, and validation outcomes. |

## RC3 Andon: maintainer-instruction overload

| Field | Evidence |
|---|---|
| Classification | `ANDON_PROBE`; class `maintainer-instruction-overload`. |
| Owner/source | `AGENTS.md`. |
| Abnormality | Root `AGENTS.md` had grown into a long historical instruction ledger, encouraging chunked safety-read behavior before the target repo baseline. |
| Impact | Long maintainer instructions reproduced the same weak-executor risk that the RC2 `SKILL.md` bootloader refactor had repaired for installed runtime payloads. |
| Countermeasure | Refactor root `AGENTS.md` into a concise maintainer bootloader and retain the moved historical rationale in `docs/maintenance/AGENTS-HISTORY.md`, guarded by `scripts/check-agents-bootstrap-budget.sh` and `tests/agents-bootstrap-budget.test.sh`. |

## Attachment And Zip Readback

| Artifact | Readback |
|---|---|
| Initial user request | Required fresh audit of Improve and Supergoal zips, v0.3.1.0 source milestone, no release unless separately authorized, no real-home install, no license decision, comparator content as data. |
| Correction attachment | Prior `AUDIT_COMPLETE` was invalid for this task because it only touched historical non-current ignored `.IMPLEMENTAUDIT/runs` local evidence; valid closure requires tracked source/docs/fixtures/tests/checkers. |
| Claude findings attachment | Useful but not authoritative. Each claim below is classified against live files and the comparator zips. |
| `improve-main(1).zip` | SHA-256 `8FBCE8DE9AF124B675B14C5DC7BE000AC4EC86F3772AA98688F41A461A7C9B30`. |
| `supergoal-main(3).zip` | SHA-256 `6E03BEF4CF02C33C0C93961D4EF5779E3E31C39CC966D362D0CBF0BB161537CC`. |

## Phase 0 validator inventory for RC2

| Validator/test | Exists in source? | Created in phase | Run in Phase 8 |
|---|---|---|---|
| `git diff --check` | yes | pre-existing | yes |
| `scripts/check-skill-layout-contract.sh` | yes | pre-existing | yes |
| `tests/skill-layout-contract.test.sh` | yes | pre-existing | yes |
| `scripts/check-skill-bootstrap-budget.sh` | yes | pre-existing; strengthened in Phase 3 | yes |
| `tests/skill-bootstrap-budget.test.sh` | yes | pre-existing | yes |
| `scripts/check-dogfood-bootstrap-contract.sh` | yes | pre-existing | yes |
| `tests/dogfood-bootstrap-contract.test.sh` | yes | pre-existing | yes |
| `scripts/check-rc2-safeguard-restoration.sh` | no at inventory | Phase 7 | yes |
| `tests/rc2-safeguard-restoration.test.sh` | no at inventory | Phase 7 | yes |
| `scripts/check-competitor-surpass-contract.sh` | yes | pre-existing | yes |
| `tests/competitor-surpass-improve.test.sh` | yes | pre-existing; strengthened in Phase 4 | yes |
| `tests/competitor-surpass-supergoal.test.sh` | yes | pre-existing; strengthened in Phase 5 | yes |
| `scripts/check-plan-quality-contract.sh` | yes | pre-existing; strengthened in Phase 4 | yes |
| `tests/plan-quality-contract.test.sh` | yes | pre-existing; strengthened in Phase 4 | yes |
| `scripts/check-installed-payload-self-contained.sh` | yes | pre-existing | yes |
| `tests/installed-payload-self-contained.test.sh` | yes | pre-existing | yes |
| `scripts/check-proof-run-currentness.sh` | yes | pre-existing | yes |
| `tests/proof-run-currentness.test.sh` | yes | pre-existing | yes |
| `tests/e2e-mini-audit-loop.test.sh` | yes | pre-existing | yes |
| `scripts/check-validation-registry.sh` | yes | pre-existing | yes |
| `scripts/verify-package.sh` | yes | pre-existing | yes |
| `scripts/build-release-asset.sh` | yes | pre-existing | yes, build phase |
| `scripts/write-release-checksums.sh` | yes | pre-existing | yes, build phase |
| `scripts/build-source-evidence-pack.sh` | no at inventory | Phase 5 | yes, source evidence pack build/test |
| `tests/source-evidence-pack.test.sh` | no at inventory | Phase 5 | yes, via Supergoal/source-evidence proof |

## Comparator Smoke Results

| Comparator | Command | Result | Classification |
|---|---|---|---|
| Improve | Inventory check for scripts/tests | No `scripts/` or `tests/` directory in the zip. | Accurate limitation; IMPLEMENTAUDIT surpasses via tracked tests/checkers. |
| Supergoal | `bash tests/claim-run.test.sh` | 23 passed, 0 failed. | Valid comparator strength. |
| Supergoal | `bash tests/repo-state.test.sh` from raw zip extract | 46 passed, 1 failed at `git check-attr` because the zip extract was not a git repo. | Environmental failure, not a source failure. |
| Supergoal | Same repo-state test from a throwaway initialized git copy | 47 passed, 0 failed. | Valid comparator strength. |

The raw-zip failure is not used as a product criticism. It is only recorded to
avoid repeating the exact mistake of treating environment-sensitive smoke as
source proof.

## Improve Matrix Verdict

Tracked source of truth:
`fixtures/competitor-surpass/improve-matrix.md`.

Canonical counts:

```text
SURPASSED: 7
COVERED: 19
REJECTED_WITH_REASON: 3
DEFERRED: 1
OWNER_DECISION: 0
UNVERIFIED: 0
FAIL: 0
```

Summary: 30 rows classified. Useful concepts are either surpassed or covered:
self-contained plans, drift checks, STOP conditions, machine-checkable
criteria, repo-content-as-data, secret redaction, issue-publication deferral,
executor isolation, review-before-merge, branch/diff classification,
reconciliation, and grounded direction planning. Read-only-by-default and root
`plans/` as canonical substrate remain rejected with reasons because they
conflict with IMPLEMENTAUDIT identity. Public issue creation is explicitly
deferred. Improve's lack of a test harness is retained only as
`REJECTED_WITH_REASON` because it is an observation, not a capability row, and
no runtime claim is made from that absence.

## Supergoal Matrix Verdict

Tracked source of truth:
`fixtures/competitor-surpass/supergoal-matrix.md`.

Canonical counts:

```text
SURPASSED: 7
COVERED: 22
REJECTED_WITH_REASON: 3
DEFERRED: 0
OWNER_DECISION: 1
UNVERIFIED: 0
FAIL: 0
```

Summary: 33 rows classified. Useful concepts are either surpassed or covered:
namespaced run roots, complete working tree vs baseline, Stage 6 self-critique,
Stage 6.5 pre-flight, single ready-to-paste `/goal`, final audit before
completion, memory privacy boundaries, adaptive phases, polish/harden pressure,
and source-tested helper scripts. The grep-light `validate-phase.sh` comparator
feature is surpassed by IMPLEMENTAUDIT's non-placeholder validator. Fixed
three-strike handoff and capped audit rounds remain rejected with reasons
because they conflict with IMPLEMENTAUDIT's no-terminal-cap Andon contract.
Adding a LICENSE file remains an explicit owner decision.

## Additional findings found during v0.3.1.0

| Finding | Classification | Evidence / closure |
|---|---|---|
| Status vocabulary mismatch made the matrices unreadable against the requested acceptance criteria. | Closed by schema repair. | Matrices now have `Canonical status` plus `Detail status`; `scripts/check-competitor-surpass-contract.sh` rejects noncanonical canonical statuses and verifies audit counts. |
| Competitor-surpass checker lacked tracked negative fixtures. | Closed by tracked fixtures. | `fixtures/competitor-surpass/negative/improve-missing-repo-content-as-data.md` and `fixtures/competitor-surpass/negative/supergoal-missing-repo-state-baseline.md` are wired through comparator tests. |
| Codex CLI dogfood was not separately recorded; install-copy smoke was being overtreated as dogfood risk. | Closed by explicit boundary. | The dogfood-status section below records install-copy smoke separately from actual Codex CLI invocation. |
| Existing all-zero requested-status counts were misleading because matrices used another vocabulary. | Closed by count summaries. | Improve and Supergoal canonical counts above are checker-verified against row status cells. |
| ANDON_PROBE: instruction-budget / installed-payload-readback-overload. | Reopened by authenticated self-dogfood and checker-hardened. | full installed-skill chunk-reading is non-evidence. `skills/implementaudit/SKILL.md` now begins dogfood with a first executable no-full-readback rule and ships `## Dogfood Bootstrap / Read Map`; `scripts/check-dogfood-bootstrap-contract.sh` now checks transcript-order negative fixtures so readback/chunking-before-baseline fails deterministically. |
| Nested Codex shell commands were rejected by policy during self-dogfood. | Closed as a runner-contract blocker, not a product closure. | `--ask-for-approval never` plus the loaded `C:\Users\theis\.codex\rules\default.rules` allowed only a narrow unrelated command set, so required repo-local validation commands such as `git rev-parse HEAD` and `bash tests/*.test.sh` were declined. `skills/implementaudit/SKILL.md` now carries a Dogfood Runner Contract that names the safe command subset and requires owner-present `--ask-for-approval on-request` or a narrow exec-policy allowlist when `never` cannot run those commands. |
| SKILL.md was still too large and manual-like after the readback guardrail patch. | Closed by bootloader diet and budget gate. | The guardrail-only patch left `skills/implementaudit/SKILL.md` at 1692 lines / 87453 bytes and added 67 lines. The diet refactor reduced it to 360 lines / 16316 bytes, moved detail ownership to existing references, and added `scripts/check-skill-bootstrap-budget.sh` plus `tests/skill-bootstrap-budget.test.sh` with a negative full-read installed-payload fixture. |
| IMPLEMENTAUDIT source skill layout is non-standard versus comparator skill repos. | Closed by source-layout migration. | Migrated canonical source with `git mv` to `skills/implementaudit/SKILL.md`, `skills/implementaudit/references/`, `skills/implementaudit/scripts/`, and `skills/implementaudit/templates/`; source manifest now points at `./skills/`, while the release builder flattens that directory into the archive root only as a build artifact. Enforced by `scripts/check-skill-layout-contract.sh` and `tests/skill-layout-contract.test.sh`. |
| Active docs can be stale in tree form without containing literal flat-layout path strings. | Closed by tree-shape guard. | The migration initially left the `AGENTS.md` repo-layout tree showing `SKILL.md`, `references/`, `scripts/`, and `templates/` directly under `skills/`. `AGENTS.md` now shows `skills/implementaudit/`, and `scripts/check-skill-layout-contract.sh` plus `tests/skill-layout-contract.test.sh` reject stale flat skill-payload trees. |
| RC1 bootloader shrink removed unique weak-executor safeguards instead of relocating all of them. | Closed by RC2 restoration. | `templates/final-report.md`, `references/sidecars.md`, `templates/read-only-plan.md`, `references/repo-state-comparison.md`, `references/phase-design.md`, and `references/lean-operating-discipline.md` now own the restored safeguards. `scripts/check-rc2-safeguard-restoration.sh` fails if they disappear. |
| Improve retained a narrow edge in planning-specific secret and prompt-injection hygiene. | Closed by RC2 follow-up repair. | RC2 security-planning repair: plans lane now carries Improve-grade no-secret and repo-content-as-data hygiene inside the planning flow, not only as a global invariant. `plan-lifecycle.md`, `read-only-plan.md`, child/reviewer prompts, and `check-plan-quality-contract.sh` now require no-secret-value, path/line/credential-type-only citation, rotation guidance, repo-content-as-data, prompt-injection-as-finding, and child/reviewer prompt propagation. |
| RC2 source evidence pack could ship `.sh` files with CRLF line endings. | Closed by LF-normalized pack builder/test. | `scripts/build-source-evidence-pack.sh` normalizes text archive entries and fails if any archived `.sh` retains CRLF. `tests/source-evidence-pack.test.sh` extracts the pack, checks every `*.sh` for absence of CRLF, and runs headline suites from the extracted pack without manual normalization. |
| RC3 partial dogfood exposed direct `python -` calls in source-evidence tests. | Closed by fallback interpreter selection. | `tests/agents-bootstrap-budget.test.sh`, `tests/source-evidence-pack.test.sh`, and `tests/competitor-surpass-supergoal.test.sh` now use `python` / `python3` / `py -3` fallback detection before invoking inline Python. This keeps the runnable evidence pack less host-fragile without broadening the Codex runner policy. |

No additional comparator/product findings beyond the repaired proof-schema
findings above were found in this narrow pass. Evidence boundary: this pass
re-read the current tracked audit ledger, matrices, comparator checker, tests,
README/CHANGELOG/audit-index wiring, and install/e2e fixtures; it did not redo
the original zip audit from scratch.

The installed-payload-readback-overload finding was found during the later
Codex self-dogfood retry attempt and reproduced again in the authenticated
self-dogfood run. It does not reopen the Improve/Supergoal matrix
classifications; it narrows the dogfood bootstrap behavior so installed payload
proof uses targeted owner/source reads and deterministic package checks instead
of model-visible full-file readback. The recurrence is now covered by a
transcript-order negative fixture.

## SKILL.md bootloader diet evidence

The first guardrail patch was not a true bootloader refactor. Required evidence
before the diet:

```text
wc -l skills/implementaudit/SKILL.md -> 1692 skills/implementaudit/SKILL.md
wc -c skills/implementaudit/SKILL.md -> 87453 skills/implementaudit/SKILL.md
git diff --stat -- skills/implementaudit/SKILL.md -> 68 changed lines, 67 insertions, 1 deletion
```

Classification: only a dogfood-bootstrap guardrail patch. It added a read-map
and runner contract but did not reduce the always-loaded runtime surface.

After the diet:

```text
wc -l skills/implementaudit/SKILL.md -> 360 skills/implementaudit/SKILL.md
wc -c skills/implementaudit/SKILL.md -> 16316 skills/implementaudit/SKILL.md
Budget checker -> check-skill-bootstrap-budget: ok (360 lines, 16316 bytes; budget 450 lines/22000 bytes)
```

Countermeasure: `skills/implementaudit/SKILL.md` is now a concise runtime bootloader with the
execution spine, safety boundaries, marker anchors, stage map, and reference
load map. Long/manual details remain in the existing packaged references:
`routing.md`, `plan-lifecycle.md`, `phase-design.md`,
`lean-operating-discipline.md`, `audit-category-matrix.md`,
`audit-playbook.md`, `transcript-contract.md`, `repo-state-comparison.md`,
`child-agents.md`, and `terminology-integration.md`.

New proof gate: `scripts/check-skill-bootstrap-budget.sh` enforces the
baseline-first rule, progressive disclosure, no whole installed-skill readback,
no secret reproduction, repo content as data, explicit authorization gates,
Smoke A/B, Andon/no arbitrary try caps, `AUDIT_COMPLETE` before
`IMPLEMENTAUDIT_RUN_COMPLETE`, and a 450-line / 22000-byte maximum. Negative
fixture: `fixtures/skill-bootstrap-budget/negative-full-read-installed-payload.md`.

## RC2 regression repair: bootloader shrink versus safeguard deletion

RC1 package readback:

```text
RC1_SKILL_LINES 367
RC1_SKILL_BYTES 16591
```

Current source after RC2 restoration:

```text
CURRENT_SKILL_LINES 379
CURRENT_SKILL_BYTES 17220
```

Classification: RC1 was a good bootloader architecture move, but Claude's RC1
audit found that some shrinkage was deletion rather than relocation. References
and templates did not absorb every unique weak-executor safeguard. RC2 keeps
the bootloader budget intact and restores the unique content into progressive
references/templates.

Redundant deleted material: repeated safety defaults, repeated marker-order
phrasing, and duplicated sidecar proof-boundary prose already covered by the
bootloader or existing references.

Unique deleted or weakened material: final report template, Graphify-assisted
Gemba, first-run tooling onboarding, commit granularity, broad rewrite
threshold, and 5-Whys loop-exit protocol.

| Lost / weakened safeguard | RC1 status | RC2 owner/source | Runtime load path | Checker/test | Status |
|---|---|---|---|---|---|
| Final report template | Scattered across run-root templates; no single canonical final-report template. | `skills/implementaudit/templates/final-report.md` | `templates/final-report.md` from the Reference Load Map when final response shape is needed. | `scripts/check-rc2-safeguard-restoration.sh`; `tests/rc2-safeguard-restoration.test.sh`; `fixtures/rc2-safeguards/negative-missing-final-report.md` | restored |
| Graphify-assisted Gemba | Present in pieces but missing the compact optional-Gemba owner. | `skills/implementaudit/references/sidecars.md`; `skills/implementaudit/references/lean-operating-discipline.md` | `references/sidecars.md` when optional sidecars/tooling matter. | `scripts/check-rc2-safeguard-restoration.sh` | restored |
| First-run tooling onboarding | Weakened by deletion from always-loaded runtime without equivalent progressive owner. | `skills/implementaudit/references/sidecars.md` | `references/sidecars.md` when optional tool detection/onboarding appears. | `scripts/check-rc2-safeguard-restoration.sh` | restored |
| Commit granularity | Guardrail mostly in AGENTS; shipped progressive owner was insufficient. | `skills/implementaudit/references/repo-state-comparison.md` | `references/repo-state-comparison.md` for final audit, cleanliness, and local git trace. | `scripts/check-rc2-safeguard-restoration.sh` | restored |
| Broad rewrite threshold | The concise bootloader named narrow scope but lost a concrete threshold. | `skills/implementaudit/references/phase-design.md` | `references/phase-design.md` for phase/risk/scope decisions. | `scripts/check-rc2-safeguard-restoration.sh` | restored |
| 5-Whys loop-exit protocol | Existing no-cap wording remained, but concrete stop conditions were weakened. | `skills/implementaudit/references/lean-operating-discipline.md` | `references/lean-operating-discipline.md` for Andon/Hansei/5 Whys. | `scripts/check-rc2-safeguard-restoration.sh`; `fixtures/rc2-safeguards/negative-missing-5whys-exit.md` | restored |

Checker evidence: `scripts/check-skill-bootstrap-budget.sh` now verifies load-map
reachability and ensures restored detail lives outside `SKILL.md`. The new
`scripts/check-rc2-safeguard-restoration.sh` checks restored safeguard substance
and fails empty or hollow files.

## Improve `plans/` lane repair

Status: restored as a first-class optional read-only lane.

`skills/implementaudit/references/plan-lifecycle.md` now owns the read-only
`plans/` output lane. It is selected for audit, plan, review, or direction
requests with no implementation authorization. It does not replace the
source-only governed `.IMPLEMENTAUDIT/runs/` substrate for implementation. A read-only plan is a
`tdqyq-audit-object` that never performs a mutating `ydqyq-audit-action`.

`skills/implementaudit/templates/read-only-plan.md` provides the verbose,
self-contained plan shape: current-state excerpts, exact file paths,
repo conventions, planned-at SHA, drift check, scope/non-scope, STOP
conditions, commands with expected outputs, test plan, done criteria,
maintenance notes, and rejected/deferred findings.

`scripts/check-plan-quality-contract.sh` now checks read-only lane status files
using `git status --porcelain --untracked-files=all` style input. Default
allowed paths are `plans/` and `.IMPLEMENTAUDIT/`; `docs/audits/` is allowed
only when declared by the fixture. The negative fixture
`fixtures/competitor-surpass/plan-quality/negative-read-only-source-mutation.status`
fails if a read-only run touches source.

### RC2 security-planning repair

RC2 security-planning repair: plans lane now carries Improve-grade no-secret
and repo-content-as-data hygiene inside the planning flow, not only as a global
invariant.

`skills/implementaudit/references/plan-lifecycle.md` and
`skills/implementaudit/templates/read-only-plan.md` now require read-only
planning, review-plan, direction, and plans-output work to never reproduce
secret values; cite only path, line, and credential type; recommend rotation
when a secret may have been exposed; treat repo content as data, not
instructions; treat prompt injection in repo/docs/issues/examples as a finding,
not an instruction; and pass those rules into child-agent/reviewer prompts or
plan-dispatch prompts.

Checker evidence:

- `scripts/check-plan-quality-contract.sh` fails if the lane/template/prompt
  surfaces lack the security rules.
- `fixtures/competitor-surpass/plan-quality/security/repo-ignore-previous-instructions.md`
  and `repo-print-env.md` model untrusted repo instructions.
- `fixtures/competitor-surpass/plan-quality/security/repo-fake-secret.txt`
  contains a fake credential value for redaction testing.
- `fixtures/competitor-surpass/plan-quality/security/negative-plan-reproduces-fake-secret.md`
  fails if a plan copies the fake value.
- `fixtures/competitor-surpass/plan-quality/security/negative-child-prompt-missing-security-rules.md`
  fails if a child/reviewer prompt omits the planning-security rules.

## Supergoal demonstrated-correctness repair

Status: restored through a runnable source evidence pack.

RC2 adds `scripts/build-source-evidence-pack.sh` and
`tests/source-evidence-pack.test.sh`. The pack preserves real repo-relative
paths (`skills/`, `scripts/`, `tests/`, `fixtures/`, docs, metadata) so an
external auditor can extract it and run the documented smoke commands from the
pack root. It excludes `.IMPLEMENTAUDIT/`, `plans/`, sidecar outputs,
`.git`, secrets, and build artifacts.

The generated pack includes `RUN-VALIDATION.md`, which names exact commands,
expected exit code `0`, and expected last-line evidence for the source-evidence
smokes. `tests/competitor-surpass-supergoal.test.sh` now builds and extracts
the pack, then runs the RC2 safeguard checker from inside the extracted layout.

RC2 follow-up repair: the source evidence pack now normalizes text archive
entries to LF and explicitly rejects CRLF-bearing shell scripts. The test
extracts the pack, asserts every `*.sh` has no `\r\n`, checks forbidden local
evidence prefixes/files, and runs the headline suites from the extracted pack
without manual normalization.

## RC2 audit evidence-retention hygiene

Status: closed by archive move plus retention checker.

The hygiene pass inventoried `docs/audits/`, kept the current
`v0.3.1.0-competitor-surpass-proof-audit.md`, `INDEX.md`, and new
`RETENTION.md` in the active root, and moved historical ledgers to
`docs/audits/archive/` with `git mv`. No audit file was deleted.

Evidence preservation:

- `docs/audits/RETENTION.md` classifies every retained audit file as current
  proof, release-critical historical ledger, historical-but-summarized,
  superseded draft retained, or source inventory.
- `docs/audits/INDEX.md` now links every archived ledger and identifies the
  active root policy.
- `scripts/check-audit-retention.sh` fails if historical audit files reappear
  in the active root, archived required ledgers are missing, `INDEX.md` lacks
  retained links, active files point at stale root paths, or generated/local RC
  artifacts are tracked.
- `tests/audit-retention.test.sh` proves the checker rejects an incomplete
  retention policy and a stale root-path reference.

Generated/local artifact boundary remains unchanged: `.IMPLEMENTAUDIT/`,
`dist/`, `graphify-out/`, `custody.db`, raw local transcripts, RC `.skill`
files, source evidence zips, and unpromoted `plans/` are not tracked source.

## Package layout / directory convention audit

| Question | Answer |
|---|---|
| What layout do Improve and Supergoal use? | IMPROVE uses a conventional name-matched skill directory: `.codex-tmp/v0310-comparators/improve/improve-main/skills/improve/SKILL.md` plus `skills/improve/references/`; its `.claude-plugin/plugin.json` has name `improve` and no overriding flat source `skills` field. Supergoal uses `.codex-tmp/v0310-comparators/supergoal/supergoal-main/skills/supergoal/SKILL.md` plus `skills/supergoal/references/`; its `.claude-plugin/plugin.json` has `name: supergoal` and `skills: "./skills/"`, and `.claude-plugin/marketplace.json` uses `source: "./"`. |
| What layout does IMPLEMENTAUDIT use? | Before this repair, IMPLEMENTAUDIT used flat source paths (`skills/SKILL.md`, `skills/references/`, `skills/scripts/`, `skills/templates/`) with `.claude-plugin/plugin.json` `skills: "./"` and marketplace `path: ".."`. It now uses `skills/implementaudit/SKILL.md`, `skills/implementaudit/references/`, `skills/implementaudit/scripts/`, and `skills/implementaudit/templates/`; source `.claude-plugin/plugin.json` uses `skills: "./skills/"`, and source marketplace metadata uses `source: "./"`. |
| Is IMPLEMENTAUDIT source layout conventional? | Yes after this migration. It now matches the comparator/common name-matched source pattern. The old flat source layout was non-standard and is no longer live behavior source. |
| Is IMPLEMENTAUDIT release archive layout conventional? | Host-specific flat archive. `scripts/build-release-asset.sh` consumes `skills/implementaudit/` as source and emits root `SKILL.md`, `references/`, `scripts/`, `templates/`, and generated archive-local `.claude-plugin/` metadata. `tests/release-asset.test.sh` and install-copy tests prove the archive must not contain nested `skills/` paths. |
| Does the current layout make Codex/Claude install simpler or harder? | Source checkout discovery is simpler and comparator-aligned because the skill is under `skills/implementaudit/`. Codex/Claude release installs remain unchanged for the built asset because the installer extracts the flat archive into the host skill directory. Manual source-copy docs were corrected to copy `skills/implementaudit/*` into the target skill directory instead of copying the parent `skills/*`. |
| Does the current layout create duplicate authority or source/package drift? | The migration reduces duplicate authority: there is no live `skills/SKILL.md`, and the layout checker rejects recreating it. Source/package drift is controlled by `scripts/build-release-asset.sh` required-source and required-archive manifests, generated archive metadata, `scripts/check-skill-layout-contract.sh`, release-asset tests, install-copy tests, and `scripts/verify-package.sh`. |
| Should v0.3.1.0 migrate layout? | Decision: migrate now. Rationale: comparator evidence supports name-matched source layout, no host/package evidence showed flat source layout was required, and the release archive can remain flat as a generated artifact where existing host tests require it. |
| What checks prevent layout drift? | `scripts/check-skill-layout-contract.sh`, `tests/skill-layout-contract.test.sh`, `scripts/build-release-asset.sh --check`, `tests/release-asset.test.sh`, `tests/release-asset-install.test.sh`, `tests/install-copy-smoke.test.sh`, `scripts/check-installed-payload-self-contained.sh`, `scripts/check-validation-registry.sh`, and `scripts/verify-package.sh`. The layout checker also rejects active-doc repo-layout trees that place `SKILL.md`, `references/`, `scripts/`, or `templates/` directly under top-level `skills/`. |

Layout finding ledger:

| Field | Value |
|---|---|
| Finding | IMPLEMENTAUDIT source skill layout is non-standard versus comparator skill repos. |
| Root cause | Flat source/package layout was treated as declared policy rather than ecosystem-compatibility evidence. |
| Countermeasure | Migrated canonical source to the conventional name-matched layout and kept flat archive layout as a generated package artifact only. |
| Evidence | Comparator manifests, package builder tests, install-copy tests, layout checker, verify-package. |
| Status | done |

Status: done.

## Evidence lane statuses

| Lane | Status | Evidence boundary |
|---|---|---|
| LOCAL_DETERMINISTIC_VALIDATION | PASS | Local checks and `bash scripts/verify-package.sh` pass from the current working tree. This is deterministic source/package evidence, not live Codex CLI dogfood. |
| PACKAGE_BUILD_AND_TEMP_INSTALL | PASS | Local `.skill` build, checksum write, temp Codex install-copy smoke, release-asset tests, and installed-payload self-containment checks pass. This proves package/build/install-copy behavior, not host model execution. |
| CODEX_CLI_TEMP_HOME_SELF_DOGFOOD | HISTORICAL_PASS_PRE_RC2_PAYLOAD / CODEX_SELF_DOGFOOD_NOT_RERUN_AFTER_RC2 | Authenticated temp-home Codex CLI reached the model from the IMPLEMENTAUDIT repo root using the temp-installed local skill before RC2 payload changes. A temp-only `rules/default.rules` profile allowed the required safe repo-local validation commands, and the nested run emitted `CODEX_CLI_POLICY_REPAIRED_AND_SELF_DOGFOOD_PROVEN`, `AUDIT_COMPLETE`, and `IMPLEMENTAUDIT_RUN_COMPLETE`. Because RC2 changes `SKILL.md`, references, templates, and package metadata, that transcript is historical evidence only for RC2 until rerun. |
| CODEX_APP_OR_WEB_SELF_DOGFOOD | NOT_RUN | No Codex app/web/cloud dogfood lane has been run or claimed in this local proof pass. |

Future-safe runner-policy note: the default local rules lane remains too narrow
for this repo's self-dogfood validation commands and was historically classified
as `CODEX_CLI_SELF_DOGFOOD_BLOCKED_BY_LOCAL_EXEC_POLICY`. A CLI proof may run
only with an explicit temp-home or repo-local Codex rules profile that permits
the required safe validation commands:

- `git status --short --branch --untracked-files=all`
- `git rev-parse HEAD`
- `git diff --check`
- `bash scripts/check-*.sh`
- `bash tests/*.test.sh`
- `bash scripts/verify-package.sh`

Do not create or install that rules profile in the real user Codex home unless
explicitly authorized. Do not weaken safety, do not use `danger-full-access`,
and do not use dangerous bypass modes as dogfood proof.

## Codex CLI temp-home self-dogfood evidence

Status: CODEX_CLI_POLICY_REPAIRED_AND_SELF_DOGFOOD_PROVEN

RC2 status: CODEX_SELF_DOGFOOD_NOT_RERUN_AFTER_RC2. RC2 changes installed
payload files after the policy-repaired transcript, so the prior self-dogfood
run is historical evidence only for RC2.

Historical aggregate labels retained for continuity and superseded by the
runner-policy proof: CODEX_DOGFOOD_BLOCKED / CODEX_SELF_DOGFOOD_BLOCKED /
CODEX_CLI_SELF_DOGFOOD_BLOCKED_BY_LOCAL_EXEC_POLICY.

| Evidence item | Status | Exact command/path/evidence |
|---|---|---|
| Local `.skill` build | Built from current working tree. | `bash scripts/build-release-asset.sh`; checksum manifest via `bash scripts/write-release-checksums.sh dist/IMPLEMENTAUDIT.skill dist/CHECKSUMS.txt`. |
| Temp Codex auth/config bootstrap | Performed without secret readback. | Copied only `auth.json` and `config.toml` by filename from `C:\Users\theis\.codex` to `C:\Users\theis\AppData\Local\Temp\codex-home-authdogfood-ffde3731391748eba286783066adf8ca`; logs list names/sizes only, not contents. The run used `--ignore-user-config` plus `-c 'service_tier="fast"'` because the copied real config's `service_tier = "flex"` was rejected by this CLI/model path. |
| Temp Codex home install-copy smoke | Verified as install-copy smoke only. | Installed with `bash scripts/install-codex-from-release.sh --asset dist/IMPLEMENTAUDIT.skill --checksum dist/CHECKSUMS.txt --codex-home /mnt/c/Users/theis/AppData/Local/Temp/codex-home-authdogfood-ffde3731391748eba286783066adf8ca --version 0.3.1`. |
| Built local `.skill` installed in temp home | Yes. | Temp Codex home: `C:\Users\theis\AppData\Local\Temp\codex-home-authdogfood-ffde3731391748eba286783066adf8ca`; installed skill path: `skills/implementaudit/SKILL.md`; installed asset SHA-256 `80e2c13e29028099575c43d47521ad6243b06b06bd3a984062f6e22b49e66b51`. |
| Actual Codex CLI invocation | Ran and reached the model, but handed off blocked. | `codex-cli 0.130.0-alpha.5`; model `gpt-5.5`; service tier override `fast`; working directory `C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT`; exit code `0` for the CLI process; nested final status remained blocked. |
| Temp target repo path | IMPLEMENTAUDIT repo itself, per corrected dogfood requirement. | `C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT`. |
| First authenticated transcript/output path | Captured. | `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-auth-full-df31f74d18d348f4a9209c2cf832aeb0\codex-exec-transcript.jsonl`; stderr path `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-auth-full-df31f74d18d348f4a9209c2cf832aeb0\codex-exec-stderr.txt`; prompt path `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-auth-full-df31f74d18d348f4a9209c2cf832aeb0\prompt.txt`; last-message path `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-auth-full-df31f74d18d348f4a9209c2cf832aeb0\codex-last-message.txt`. |
| First authenticated blocker evidence | Nested CLI shell policy rejection plus readback recurrence. | Stderr records `blocked by policy` for `git rev-parse HEAD`, `codex.exe --version`, `codex.exe exec --help`, and focused `bash tests/*.test.sh` commands. Transcript item 1 reads `...\skills\implementaudit\SKILL.md` before the baseline, reproducing `installed-payload-readback-overload`. |
| First authenticated marker evidence | Handoff marker only. | Last message began `AUDIT_HANDOFF` and reported `## Verdict` `BLOCKED`; no terminal completion markers were produced. |
| Repaired self-dogfood transcript/output path | Captured. | `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-repair-10be15bb72e948768218eaac0f95ab80\codex-exec-transcript.jsonl`; stderr path `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-repair-10be15bb72e948768218eaac0f95ab80\codex-exec-stderr.txt`; prompt path `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-repair-10be15bb72e948768218eaac0f95ab80\prompt.txt`; last-message path `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-repair-10be15bb72e948768218eaac0f95ab80\codex-last-message.txt`. |
| Repaired self-dogfood order evidence | Baseline-first behavior proven, but policy still blocked closure. | Transcript item 1 runs `git status --short --branch --untracked-files=all`; item 3 attempts `git rev-parse HEAD` and receives `rejected: blocked by policy`. No installed-skill readback command precedes baseline. |
| Policy root cause | Runner mode and exec-policy mismatch. | The nested run used `--ask-for-approval never`; Codex help states `never` returns execution failures immediately. The loaded `C:\Users\theis\.codex\rules\default.rules` did not allow this repo's validation commands, so they could not be escalated. |
| Required runner mode | Safe validation requires permission for a narrow local command set. | The Dogfood Runner Contract allows baseline/read-only checks, targeted owner/source reads, and repo-local `git diff --check`, `bash scripts/check-*.sh`, `bash tests/*.test.sh`, and `bash scripts/verify-package.sh`; if `never` rejects those commands, the dogfood run must use owner-present `--ask-for-approval on-request` with `--sandbox workspace-write`, or an explicit narrow exec-policy allowlist. It must not use `--dangerously-bypass-approvals-and-sandbox` or `danger-full-access`. |
| Final marker evidence | Blocked marker only. | The repaired run's last message begins `CODEX_SELF_DOGFOOD_BLOCKED`; no terminal completion markers were produced by the nested Codex CLI run. |
| Post-layout authenticated temp-home bootstrap | Performed without secret readback. | Fresh temp home `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328\codex-home`; copied only `auth.json`, `config.toml`, and `installation_id` from `C:\Users\theis\.codex`; logs list filenames/sizes only. |
| Post-layout installed local `.skill` | Built and installed in temp home. | `bash scripts/build-release-asset.sh`; `bash scripts/write-release-checksums.sh dist/IMPLEMENTAUDIT.skill dist/CHECKSUMS.txt`; `bash scripts/install-codex-from-release.sh --asset dist/IMPLEMENTAUDIT.skill --checksum dist/CHECKSUMS.txt --codex-home /mnt/c/Users/theis/AppData/Local/Temp/codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328/codex-home --version 0.3.1`; installed SHA-256 `4c1e565dfaee52d71eeb948762db51b4125674478abb8e190add6d53495eb68a`. |
| Post-layout startup blocker probes | Captured as non-evidence attempts before the successful model invocation. | `run-output\codex-exec-transcript.jsonl` failed before model work with `Unsupported service_tier: flex`; `run-output-fast\codex-exec-transcript.jsonl` failed before model work with `The 'gpt-5.3-codex' model is not supported when using Codex with a ChatGPT account.` |
| Post-layout actual Codex CLI invocation | Ran and reached the model, but handed off blocked. | `codex-cli 0.130.0-alpha.5`; command used explicit `-m gpt-5.5`, `--ignore-user-config`, and `-c 'service_tier="fast"'`; working directory `C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT`; transcript `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328\run-output-fast-gpt55\codex-exec-transcript.jsonl`; last message `...\run-output-fast-gpt55\codex-last-message.txt`; exit code `0`. |
| Post-layout order evidence | Baseline-first behavior preserved; no installed-payload chunking occurred before baseline. | Transcript starts with an agent message committing to baseline-first, then repo-baseline commands (`git rev-parse --show-toplevel`, `git status --short`, `git diff --name-only`, `git ls-files -o --exclude-standard`) before targeted reads of `AGENTS.md`, `skills/implementaudit/SKILL.md`, references, scripts, docs, and tests. |
| Post-layout policy blocker evidence | Still blocked. | Required safe commands were declined by policy: `git rev-parse HEAD`, `bash scripts/check-skill-layout-contract.sh`, `bash scripts/check-dogfood-bootstrap-contract.sh`, and `bash scripts/check-skill-bootstrap-budget.sh`. Later temp-home/Codex discovery commands were also declined: `$env:CODEX_HOME`, temp-home listings, and `Get-Command codex`. |
| Post-layout final marker evidence | Blocked marker only. | Last message begins `Blocked handoff`; it explicitly states it did not update `CODEX_SELF_DOGFOOD_BLOCKED` to `CODEX_SELF_DOGFOOD_PROVEN` and did not print terminal completion markers. |
| Runner-policy root cause | Fixed by temp-only rule profile. | The default local `rules/default.rules` did not allow the nested Codex CLI's PowerShell-wrapped safe validation commands, and the initial authenticated temp home had no repo-specific allowlist. The blocker was therefore a local runner-policy/profile gap, not an IMPLEMENTAUDIT repo correctness failure. |
| Runner-policy files inspected | Secret-safe inspection only. | Blocked transcript: `C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328\run-output-fast-gpt55\codex-exec-transcript.jsonl`; real Codex home filenames under `C:\Users\theis\.codex`; temp Codex home filenames; real `rules/default.rules`; `codex --help`, `codex exec --help`, and `codex sandbox --help`. Secret-bearing `auth.json` and raw config/auth contents were not printed. |
| Temp-only runner policy created | Yes. | `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\codex-home\rules\default.rules`. Real global rules were untouched. |
| Temp runner policy allowed commands | Narrow repo-local validation surface only. | `git status --short --branch --untracked-files=all`; `git rev-parse HEAD`; `git rev-parse --show-toplevel`; `git diff --check`; `git diff --stat`; `git diff --name-only`; `git ls-files -o --exclude-standard`; repo-local `bash scripts/check-*.sh`; repo-local `bash tests/*.test.sh`; `bash scripts/build-release-asset.sh`; `bash scripts/write-release-checksums.sh dist/IMPLEMENTAUDIT.skill dist/CHECKSUMS.txt`; `bash scripts/install-codex-from-release.sh --asset dist/IMPLEMENTAUDIT.skill --checksum dist/CHECKSUMS.txt --codex-home /mnt/c/Users/theis/AppData/Local/Temp/codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817/codex-home --version 0.3.1`; `bash scripts/verify-package.sh`. The file also contained the exact PowerShell-wrapper forms Codex emitted. |
| Temp runner policy denied commands | Denied by omission. | No allow rules were added for `git commit`, `git push`, `git tag`, `gh issue create`, `gh release create`, real-home install, credential printing, `danger-full-access`, `dangerously-bypass`, or destructive deletion outside temp dirs. |
| Policy probe | Passed. | Transcript `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\policy-probe-transcript.jsonl` proved `git rev-parse HEAD` returned `c86acfe6a255a492a4cf125fdb05a31d08cb5cc3` and `bash scripts/check-skill-layout-contract.sh` returned `check-skill-layout-contract: ok`. |
| Policy-repaired build/install | Passed. | `bash scripts/build-release-asset.sh`; `bash scripts/write-release-checksums.sh dist/IMPLEMENTAUDIT.skill dist/CHECKSUMS.txt`; `bash scripts/install-codex-from-release.sh --asset dist/IMPLEMENTAUDIT.skill --checksum dist/CHECKSUMS.txt --codex-home /mnt/c/Users/theis/AppData/Local/Temp/codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817/codex-home --version 0.3.1`; installed SHA-256 `4c1e565dfaee52d71eeb948762db51b4125674478abb8e190add6d53495eb68a`. |
| Policy-repaired self-dogfood transcript/output path | Captured. | Transcript `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-transcript.jsonl`; stderr `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-stderr.txt`; prompt `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-prompt.txt`; last message `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-last-message.txt`; exit-code file `C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-exit-code.txt`. |
| Policy-repaired order evidence | Proven. | The nested final message states the first command was `git status --short --branch --untracked-files=all`, followed by `git rev-parse HEAD` at `c86acfe6a255a492a4cf125fdb05a31d08cb5cc3`; it also states installed-payload readback before baseline did not occur. |
| Policy-repaired validation evidence | Passed. | The nested run executed the required validation commands. `bash scripts/verify-package.sh` first hit a tool timeout around 124 seconds, then the same command was retried with a longer timeout and passed with `verify-package: ok`. |
| Policy-repaired final marker evidence | Terminal markers produced. | Last message contains `CODEX_CLI_POLICY_REPAIRED_AND_SELF_DOGFOOD_PROVEN`, then `AUDIT_COMPLETE`, then `IMPLEMENTAUDIT_RUN_COMPLETE`. |

Exact Codex CLI command attempted:

```powershell
$env:CODEX_HOME='C:\Users\theis\AppData\Local\Temp\codex-home-authdogfood-ffde3731391748eba286783066adf8ca'; Get-Content -Raw 'C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-auth-full-df31f74d18d348f4a9209c2cf832aeb0\prompt.txt' | & 'C:\Users\theis\AppData\Local\OpenAI\Codex\bin\codex.exe' --cd 'C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT' --sandbox workspace-write --ask-for-approval never -m gpt-5.5 exec --ignore-user-config -c 'service_tier="fast"' --json --output-last-message 'C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-auth-full-df31f74d18d348f4a9209c2cf832aeb0\codex-last-message.txt' -
```

Exact repaired Codex CLI command attempted:

```powershell
$env:CODEX_HOME='C:\Users\theis\AppData\Local\Temp\codex-home-authdogfood-ffde3731391748eba286783066adf8ca'; Get-Content -Raw 'C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-repair-10be15bb72e948768218eaac0f95ab80\prompt.txt' | & 'C:\Users\theis\AppData\Local\OpenAI\Codex\bin\codex.exe' --cd 'C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT' --sandbox workspace-write --ask-for-approval on-request -m gpt-5.5 exec --ignore-user-config -c 'service_tier="fast"' --json --output-last-message 'C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-repair-10be15bb72e948768218eaac0f95ab80\codex-last-message.txt' -
```

Exact post-layout Codex CLI command attempted:

```powershell
$env:CODEX_HOME='C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328\codex-home'; Get-Content -Raw 'C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328\run-output-fast-gpt55\prompt.txt' | & 'C:\Users\theis\AppData\Local\OpenAI\Codex\bin\codex.exe' --cd 'C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT' --sandbox workspace-write --ask-for-approval never -m gpt-5.5 exec --ignore-user-config -c 'service_tier="fast"' --json --output-last-message 'C:\Users\theis\AppData\Local\Temp\codex-self-dogfood-layout-427d98c680414dcabf8d7e13e989d328\run-output-fast-gpt55\codex-last-message.txt' -
```

Exact policy-repaired Codex CLI command:

```powershell
$env:CODEX_HOME = "C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\codex-home"
Get-Content -Raw -LiteralPath "C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-prompt.txt" | & "C:\Users\theis\AppData\Local\OpenAI\Codex\bin\codex.exe" --cd "C:\workspace\ai\improveimplementaudit\IMPLEMENTAUDIT" --sandbox workspace-write --ask-for-approval never -m gpt-5.5 exec --ignore-user-config -c 'service_tier="fast"' --json --output-last-message "C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-last-message.txt" - > "C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-transcript.jsonl" 2> "C:\Users\theis\AppData\Local\Temp\codex-policy-dogfood-056508276ad049bb9259e9ef8f1c2817\run-output\self-dogfood-policy-stderr.txt"
```

Bash discovery note: `command -v codex` did not produce a usable Bash command,
and the subsequent Bash `codex --version`, `codex --help`, `codex exec --help`,
and `codex run --help` probes reported `codex: command not found`. Windows
PowerShell discovery found `C:\Users\theis\AppData\Local\OpenAI\Codex\bin\codex.exe`,
and its help identified `codex exec` as the safe noninteractive subcommand.

## Codex self-dogfood Andon

| Field | Evidence |
|---|---|
| Marker | ANDON_PROBE |
| Class | instruction-budget / installed-payload-readback-overload |
| Abnormality | The executor attempted to chunk-read the whole installed skill before completing the requested baseline and CLI discovery sequence. |
| Impact | This reproduced the criticism that IMPLEMENTAUDIT's instruction surface can be too heavy for executors and can encourage protocol-reading instead of protocol-execution. |
| Owner/source | `skills/implementaudit/SKILL.md`, installed package read path, and this proof audit. |
| Countermeasure | Added `## Dogfood Bootstrap / Read Map` to `skills/implementaudit/SKILL.md` and checker-backed it with `scripts/check-dogfood-bootstrap-contract.sh` plus `tests/dogfood-bootstrap-contract.test.sh`. |
| Boundary | Full installed-skill chunk-reading is non-evidence for dogfood proof unless a specific owner/source section is the audit target. |
| First retry status | CODEX_SELF_DOGFOOD_BLOCKED because the unauthenticated temp `CODEX_HOME` failed with `401 Unauthorized: Missing bearer or basic authentication`. |
| Authenticated retry status | CODEX_SELF_DOGFOOD_BLOCKED after safe temp-home auth/config bootstrap because the nested Codex CLI rejected required shell validation commands by policy and repeated installed skill readback before baseline. |
| Repaired retry status | CODEX_SELF_DOGFOOD_BLOCKED after the Dogfood Bootstrap / Read Map repair because the nested run now started with the required `git status` baseline and did not read/chunk the installed skill before baseline, but `git rev-parse HEAD` was still rejected by policy. |
| Post-diet/layout retry status | CODEX_CLI_SELF_DOGFOOD_BLOCKED_BY_LOCAL_EXEC_POLICY after the bootloader diet and layout migration. The nested run preserved baseline-first behavior and did not chunk-read the installed payload first, but required safe repo-local validation commands were still rejected by local exec policy under `--ask-for-approval never`. |
| Runner-policy retry status | CODEX_CLI_POLICY_REPAIRED_AND_SELF_DOGFOOD_PROVEN after an explicitly authorized temp-home `rules/default.rules` profile allowed only the safe repo-local validation surface. The nested run preserved baseline-first behavior, did not chunk-read the installed payload before baseline, ran validation, and emitted terminal completion markers. |
| Runner contract | Added after the authenticated retry. It records the safe local command subset and the required invocation boundary when `--ask-for-approval never` cannot execute those commands under the loaded exec policy. |
| Regression fixtures | `fixtures/competitor-surpass/dogfood-bootstrap/negative/installed-readback-before-baseline-transcript.jsonl` and `fixtures/competitor-surpass/dogfood-bootstrap/negative/chunking-readback-before-baseline-transcript.jsonl` prove transcript-order negative fixtures fail when readback or chunking precedes baseline. |

## Deterministic mini audit-loop proof

| Item | Evidence |
|---|---|
| Phase fixture validates. | `bash tests/e2e-mini-audit-loop.test.sh` runs `bash skills/implementaudit/scripts/validate-phase.sh fixtures/e2e-mini-audit-loop/phase-1.md`. |
| Synthetic target repo is created. | The test initializes a temporary git repo under `mktemp -d`, commits a baseline README, writes `src-mini-proof.txt`, and verifies it through copied `repo-state.sh`. |
| Final marker evidence exists. | `fixtures/e2e-mini-audit-loop/EXPECTED-TRANSCRIPT.md` contains `AUDIT_COMPLETE` before `IMPLEMENTAUDIT_RUN_COMPLETE`. |
| Boundary is explicit. | The transcript fixture states it is deterministic source repo fixture evidence only and does not claim a live host model completed a real run. |

## Issue-publication decision

Decision: deferred.

`--issues` / public issue creation is not implemented into `/implementaudit` in
this source milestone. The user's instruction explicitly says not to implement
`--issues`; public issue creation remains behind a future explicit publication
gate.

## License decision

Decision: OWNER_DECISION.

No license was selected or added. The Supergoal comparator's `LICENSE` row is
recorded as `OWNER_DECISION`, and this source milestone does not choose a
license on the owner's behalf.

## Remaining deferrals and owner decisions

| Type | Matrix row | Decision | Rationale |
|---|---|---|---|
| DEFERRED | IMP-24 | Issue publication with `gh issue create`. | Public issue creation is a separate publication gate and was explicitly not authorized. |
| OWNER_DECISION | SG-27 | Add/select a LICENSE file. | License selection is an owner/legal decision; no license was selected in this run. |

## Claude finding verdicts

| Claude claim | Verdict | Evidence |
|---|---|---|
| Improve is compact, read-only, and strong at plans. | Accurate for the zip. | `skills/improve/SKILL.md`, plan template, no scripts/tests in inventory. |
| Improve has no test harness. | Accurate. | Zip inventory. |
| Supergoal is the strongest shippable product. | Mostly accurate as comparator packaging; not a reason to copy terminal caps. | README, AGENTS, scripts, tests. |
| Supergoal repo-state suite is 46/47 or 68/69 due to git environment. | Accurate in spirit; verified as 46/47 raw zip and 47/47 in a git copy. | Comparator smoke results above. |
| Supergoal `validate-phase.sh` is grep-only. | Accurate. | Comparator script readback. |
| Supergoal fixed three-strike and audit-round caps are weaknesses. | Accurate for IMPLEMENTAUDIT identity. | Rejected by native no-terminal-cap contract. |
| IMPLEMENTAUDIT packaging/installability is weak. | Stale against current repo. | Current repo has package builder, install scripts, release-asset tests, package-shape checks, and pre-patch `verify-package: ok`. |
| IMPLEMENTAUDIT has zero shipped tests. | Stale against current repo. | Pre-patch package verification ran 32 registered tests. |
| Graphify/ActiveGraph are prominent. | Accurate but bounded. | They are optional, non-proof sidecars; Markdown fallback remains valid. |
| Generic-runner guard needs strength. | Partly valid historically; current source already has package-shape, host-claim, no-terminal-cap, agent-eval, and identity gates. | Existing checkers plus this v0.3.1.0 proof set. |

## A-K Closure Ledger

| ID | Required repair class | Closure |
|---|---|---|
| A | Reopen invalid prior closure. | Done in this ledger: previous ignored run-root-only result is marked invalid for this task. |
| B | Freshly audit both zips. | Done: matrices and smoke results are tracked. |
| C | Classify Claude claims. | Done: table above separates accurate, stale, invalid, and deferred claims. |
| D | Add competitor-surpass proof. | Done: `scripts/check-competitor-surpass-contract.sh` and matrix tests. |
| E | Add plan-quality gates. | Done: `scripts/check-plan-quality-contract.sh` and positive/negative tests. |
| F | Add stale-proof anti-repeat gate. | Done: `scripts/check-proof-run-currentness.sh`. |
| G | Add installed-payload self-containment gate. | Done: `scripts/check-installed-payload-self-contained.sh`. |
| H | Add deterministic e2e mini audit loop. | Done: `tests/e2e-mini-audit-loop.test.sh` and source repo fixture. |
| I | Bump manifest for shipped source changes. | Done: source milestone maps to manifest `0.3.1`. |
| J | Wire validation registries. | Done through `verify-package.sh` and `.github/workflows/validate.yml`; registry checker enforces parity. |
| K | Preserve release/provenance and real-home-install boundaries. | Done: no such action was performed or claimed. |

## Stale Run-Root Evidence Rule

Stale run-root evidence is historical non-current evidence unless the current
run validates the run root and ties it to tracked source closure. The earlier
historical non-current `.IMPLEMENTAUDIT/runs/v0300-improve-parity-rQsOui/` path
is therefore source-only context for this milestone, not proof that
v0.3.1.0 tracked source was already closed.

## Source Milestone Evidence

Expected local proof commands for this milestone:

- `bash scripts/check-competitor-surpass-contract.sh --matrix all`
- `bash scripts/check-plan-quality-contract.sh`
- `bash scripts/check-installed-payload-self-contained.sh`
- `bash scripts/check-proof-run-currentness.sh`
- `bash scripts/check-rc2-safeguard-restoration.sh`
- `bash tests/rc2-safeguard-restoration.test.sh`
- `bash tests/source-evidence-pack.test.sh`
- `bash tests/e2e-mini-audit-loop.test.sh`
- `bash scripts/check-validation-registry.sh`
- `bash scripts/build-release-asset.sh --check`
- `bash scripts/verify-package.sh`

Successful completion of those commands proves the source checkout and
source-built package contract only. It does not prove a GitHub release,
marketplace publication, real host install, provenance, or issue publication.
