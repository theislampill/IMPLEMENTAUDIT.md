# v0.4.0.0 release report

Release title: **State Synthesis Substrate Engineering: Evolved-SSDDRFCSS**
Compact label: **S³E**
Runtime/plugin identity: `0.4.0`
Public identity: `v0.4.0.0`
State: **PREPUBLICATION CANDIDATE**

## Current evidence boundary

This report is the exact evidence owner for the v0.4.0.0 candidate and any later authorised publication. At source-composition time, no v0.4.0.0 tag, GitHub Release, public asset, Pages deployment, public download, marketplace state, native Codex or Claude plugin load, provenance, signature, attestation, SBOM, licence, or universal host behaviour is claimed.

The release integrates Evolved-LAW, Evolved-CSS, Evolved-SSD, and Evolved-DRF as property-level genealogy for one native state-synthesis substrate. It adds no lineage selector, nine runtimes, methodology conformance, fixed ceremony, or universal effectiveness claim.

The final research denominator is 552/552 disposition accounting, not 552 source changes. Exact adopted, sharpened, rejected, residual, unresolved, and owner-decision counts remain governed by the retained central composition; public prose does not reconstruct them from lineage research.

## Package architecture

The release architecture is one atomic, version-coherent dual-host plugin
package. `IMPLEMENTAUDIT.plugin.zip` is its canonical generated artifact. Equal
`.codex-plugin/plugin.json` and `.claude-plugin/plugin.json` manifests route the
same `./skills/` tree. `/implementaudit` remains the sole stable public/default
governor, v0.4 ships zero child skills, and its references, templates, and
helper scripts remain one shared deterministic substrate.

`IMPLEMENTAUDIT.skill` is a generated, checksummed flattened standalone
compatibility artifact from the same source identity. It is not the canonical
topology, a second independently versioned package, or evidence of native host
loading. The package contract is `package/implementaudit-package.json`; final
artifact inventories, members, bytes, and SHA-256 values must come from the
fresh exact-candidate build and later public readback.

## Candidate qualification ledger

| Evidence leg | Current source state | Exact receipt |
| --- | --- | --- |
| Candidate commit and tree | a local candidate commit exists; final-main integration, exact-current independent review and hosted qualification remain pending | exact local identities belong in the append-only author/review receipts rather than a self-referential source row |
| Focused source checks | passed for the local candidate; every amended candidate requires fresh exact-current review and hosted qualification | docs portal, census, semantic preservation, public-claim, README TOC, registry, package-shape, asset-budget, identity and held-out checks have local receipts |
| Package contract and source topology | one atomic dual-host package; one required skill; shared deterministic substrate | local source state names canonical plugin and standalone compatibility projections; exact-current independent qualification remains pending |
| Canonical plugin artifact | deterministic build, inventory, extraction, and budget evidence must be refreshed on the final exact candidate; no public asset is claimed | `IMPLEMENTAUDIT.plugin.zip`; exact members, bytes, SHA-256, source commit/tree, and clean-state binding `PENDING` |
| Standalone compatibility artifact | deterministic build, inventory, extraction, and budget evidence must be refreshed on the final exact candidate; no public asset is claimed | `IMPLEMENTAUDIT.skill`; exact members, bytes, SHA-256, source commit/tree, and clean-state binding `PENDING` |
| Isolated staged plugin projection | local Codex and Claude staged-copy, update, rollback, inventory, and post-readback checks remain separate from native host loading | final exact-candidate receipts `PENDING` |
| Standalone temporary-install projection | local Codex and Claude compatibility-copy checks remain separate from canonical plugin and native host loading | final exact-candidate receipts `PENDING` |
| Final self-dogfood ActiveGraph | mandatory for this repository's governed self-dogfood only; generic use remains optional and non-authoritative | latest published release must be independently resolved, identity/hash/provenance verified, isolated, exact-state-bound, and recorded at execution time; `PENDING` |
| Native Codex/Claude host proof | package/manifests and staged copy do not establish discovery or invocation | `PENDING` |
| Hosted `validate` | not run for final SHA | `PENDING` |
| `Deploy docs portal to GitHub Pages` | not run for final SHA | `PENDING` |
| Tag and GitHub Release | not authorised in this source-composition step | `PENDING` |
| Fresh public download and install | impossible before publication | `PENDING` |
| Tracker terminal census | pending separate owner-authorised mutation/readback | `PENDING` |

## Publication readback contract

After separate owner authority and only from independently accepted final
`main`, bind the annotated `v0.4.0.0` tag to the exact qualified commit/tree;
use GitHub Release name **State Synthesis Substrate Engineering:
Evolved-SSDDRFCSS**; attach exactly `IMPLEMENTAUDIT.plugin.zip`,
`IMPLEMENTAUDIT.skill`, and `CHECKSUMS.txt`; read back the API identity and asset
metadata; download all three into a fresh empty directory; verify both checksums
and exact inventories; repeat the qualified isolated plugin and standalone
installation/readback controls; then read all declared Pages routes from the
deployed site. Native host loading requires its own receipt. Command success
alone is not public proof.

Historical v0.3.3.3 receipts remain append-only in the prior release report and changelog.
