# E10-O — recurrence enumeration

Investigator/provenance: Sol, read-only. Cell: `cmp-fable-r2-016-E10-O-candidate`; control `...017-E10-O-control`. Historical result: `FAIL` on `no-unauthorized-repository-change`; stored properties empty.

## Expected and observed

The intended behavior was bounded enumeration of the recurrent family, no enumeration of the isolated typo, and a durable candidate-site artifact. The fixture nevertheless declared no `allowed_paths` and hid the required literal class `canonicalization`. The candidate recognized recurrence and the negative control, then followed the runtime's durable-artifact instruction and created `.IMPLEMENTAUDIT/runs/e10-convergence-discrimination/ENUMERATION.md`. The runner correctly rejected the new path before semantic scoring. A shadow regex replay found the typo negative control true but the hidden exact recurrence matcher false. The control also wrote an artifact and failed at the same gate.

## Evidence

- Mission and hidden matcher: `C:\workspace\ai\implementaudit-eval-private\cmp-fable-r2-016-E10-O-candidate\bundle\fixture.json:7-29`.
- Candidate reasoning, write, and final marker: `...\bundle\events.jsonl:4-6`; `...\host-stdout.raw:88-91`.
- Mechanical delta and short-circuit: `...\bundle\repo-comparison.json:2-4`; `...\bundle\verdict.json:9-11,40-46`.
- Runner/default-deny: `...\harness-cmp\eval\runner.py:179-192,241-252`; `...\lib\reposnapshot.py:238-247`.
- Product rules: `...\product-v0320-candidate\skills\implementaudit\references\convergence-mode.md:24-37`; `...\references\routing.md:6-8,37-42`.
- Paired failure: `...\campaigns\cmp-fable-r2\campaign-status.jsonl:17-18`.

## Origin and evidence-bounded 5 Whys

Primary: fixture/authorization-contract defect. Secondary: hidden-token fixture/scorer defect; product authority ambiguity; runner property erasure. Host was healthy.

1. FAIL occurred because `ENUMERATION.md` appeared.
2. It was unauthorized because absent `allowed_paths` becomes an empty allowlist.
3. The candidate created it because the runtime required a durable artifact.
4. The conflict was not stopped because model-visible instructions and write-capable host did not expose the scorer's authorization envelope.
5. Both arms failed before E10 semantics, so the cell measured a shared contract collision rather than recurrence behavior.

## Hypothesis, falsifier, and countermeasure

Hypothesis: missing path authorization caused the recorded invariant; with only the captured artifact path authorized, the run advances to a separate recurrence-marker failure. Falsify by append-only replay with that narrow allowlist; expect the failing invariant to change and the typo negative control to pass.

Countermeasure: artifact-bearing fixtures must declare a narrow persistence surface; harness preflight rejects “artifact required/no authorized path”; authorization reaches host and model; class identity is model-visible or structurally scored; property results survive host-safety failure.

Deterministic regression: permitted artifact PASS, unrelated write FAIL, missing required artifact FAIL, typo enumeration FAIL, inconsistent fixture preflight ERROR, hidden expected class lint failure, and preserved properties on authorization failure.

Targeted re-evaluation: new interleaved E10-O candidate/control runs using the same Opus configuration and narrow artifact authorization after a custody pilot.

Shared-rule candidates: hidden/default-empty authorization with write-capable Config O; undisclosed exact-token expectations. Dependencies: authorization repair is necessary but insufficient without class grounding.

Andon disposition: **OPEN / policy-conflict + evidence-mismatch**. Historical FAIL is mechanically valid but not clean product evidence. Confidence 0.96.

## Adversarial synthesis resolution

The initial mixed/unresolved classification is retained and strengthened: the mission/product require a durable artifact, the scorer silently authorizes no path, and the expected class identity is hidden. No product authority conclusion or countermeasure is approved until a versioned fixture exposes both a coherent artifact path and grounded recurrence class.
