# v0.3.0.0 Local-Package Dogfood Audit

Audit object for the v0.3.0.0 local `.skill` package dogfood and conditional
release. Produced by running `/implementaudit` (installed packaged skill) on
this repo. Optional sidecars used: Graphify (orientation), ActiveGraph
(custody). Neither is treated as correctness proof.

- Date: 2026-06-13
- Baseline ref (HEAD at run start): `2663c9995aec24ca5cac5577e0ad422a8a3ff525`
- Branch: `main`
- Run root (gitignored custody/orientation): `.IMPLEMENTAUDIT/runs/v0300-package-dogfood-RWZD5m`

---

## 1. Verdict

`DOGFOOD_REPAIR_COMMITTED` → release gate evaluated after repair.

One real release-gate finding (F1) was surfaced **by dogfooding on a Windows +
ActiveGraph machine** and was not visible to Linux CI; it was repaired at
owner/source, and the master package gate now passes. The installed package was
proven self-sufficient (no source-repo reach-back) in a fresh consumer repo, the
negative-boundary probes all held, and the self-maintenance audit found no
unresolved P0/P1 release blockers. Sections 11–13 record the package rebuild,
remote gate, and final release-gate verdict.

## 2. Package identity and checksum

| Field | Value |
|---|---|
| Local asset | `C:\workspace\IMPLEMENTAUDIT.skill` |
| Format | ZIP archive (UNIX v2.0), entries timestamped `1980-01-01` (deterministic) |
| Size | 112509 bytes |
| SHA-256 (pre-rebuild) | `b208da08b2ff10e68c955dfb08557a57f433fdd8c7af27bbcc4a2bf99fd9e243` |
| Entry count | 33 files |
| Payload shape | `SKILL.md` at root; `references/` (12), `scripts/` (9), `templates/` (9), `.claude-plugin/` (2) |
| Packaged manifest version | `0.3.0` (matches repo `.claude-plugin/plugin.json` → **no dogfood version skew**) |

Boundary checks on the extracted package:
- No source-repo absolute path leak (`improveimplementaudit` absent from payload).
- No bare `skills/scripts/` helper *invocation*; the one `skills/scripts/...`
  mention (`references/plan-lifecycle.md`) is explicitly labeled
  "The source repo helper is …" — a labeled reference, not a dangling
  instruction (consistent with `verify-package.sh` path-integrity gate, which
  flags unlabeled `fixtures/`, `tests/`, `scripts/check-`, `skills/scripts/`).
- Required runtime references/templates/scripts all present in the payload.
- No `fixtures/`, `tests/`, root `scripts/check-*`, README/AGENTS/CHANGELOG, or
  `.IMPLEMENTAUDIT/` run artifacts shipped — source-only material stays repo-side.

## 3. Consumer install dogfood

A fresh disposable consumer repo was created at `C:\workspace\ia-dogfood-consumer`
(git-initialized, unrelated to the source repo). The local package was extracted
into the consumer's own skill directory `.host-skills/implementaudit/` (SKILL.md
at root, `references/`, `scripts/`, `templates/`, `.claude-plugin/`).

An **isolated subagent** with no knowledge of the source repo executed the
install dogfood under hard isolation rules (may only touch the install dir and
the consumer repo; must log every file read; must record any source-repo
reach-back as a blocking finding).

Result: **PASS**.
- **Source-repo reach-back: NONE.** The agent never opened, listed, or relied on
  anything under `C:\workspace\ai\improveimplementaudit`. The installed package
  drove the entire run.
- File-read log confined to the install dir (`SKILL.md`, `references/routing.md`,
  `references/audit-category-matrix.md`, `references/transcript-contract.md`,
  `references/lean-operating-discipline.md`) and the consumer repo files.
- Path resolution: after setting `IMPLEMENTAUDIT_SKILL_DIR` to the install dir
  (as SKILL.md Stage 0 instructs), `${IMPLEMENTAUDIT_SKILL_DIR}/scripts|references|templates`
  all resolved. See Finding F2 for the documented `:-skills` default caveat.

## 4. Consumer runtime scenario dogfood

Brownfield scenario: tinycalc repo where `README.md` claims `add(a, b)` returns
the **sum** and that `python test_calc.py` passes, but `calc.py` had
`def add(a, b): return a - b` (subtraction), so `test_add` failed.

The installed runtime produced the required flow, in order:

| Required step | Result |
|---|---|
| Instruction hierarchy / safety read | §0 read; consumer has no AGENTS.md; default "No commit/push/tag/release" stance held |
| Input validation | Valid audit artifact (explicit `/implementaudit` + concrete finding) |
| Invocation shape | **Direct governance** (concrete bounded defect) |
| Route | **Brownfield** (existing owner/source + test + doc) |
| DMAIC vs DMADV | **DMAIC** (existing defect / broken check) |
| Owner/source | `calc.py:6` `return a - b` (the real bug; README + test were already correct and left untouched) |
| Smoke A (pre-mutation) | `python test_calc.py` → AssertionError, exit 1; classified `target` |
| Implementation | `return a - b` → `return a + b` (narrowest owner/source patch) |
| Smoke B (post) | `python test_calc.py` → "all tests passed", exit 0; no regression |
| Plan Closure / Control | 1 item → `done`; existing `test_add` is the regression guard |
| Final audit | `AUDIT_START → AUDIT_VERIFY → AUDIT_COMPLETE → IMPLEMENTAUDIT_RUN_COMPLETE` |

`AUDIT_COMPLETE` was printed before `IMPLEMENTAUDIT_RUN_COMPLETE`. README's sum
claim was reconciled against code (no symptom-patching of the doc or test). No
hard-failure conditions from the skill's own list were triggered.

## 5. Self-maintenance dogfood

The installed package was used to audit whether the IMPLEMENTAUDIT repo itself is
v0.3.0.0 release-ready from the package/install boundary, respecting `AGENTS.md`
and `SKILL.md`, with Graphify for terrain only and ActiveGraph for custody only.

Findings:
- **Version claims match evidence.** README:632 pins "Current project milestone:
  `v0.3.0.0`. Plugin manifest version: `0.3.0`." against the live manifest
  (V0290-README-VERSION-CLAIM-PINNED satisfied). CHANGELOG maps the `v0.3.0.0`
  source milestone to manifest `0.3.0`.
- **No stale claims / no donor-lane identity.** v0.2.9.0 appears only as
  *historical context* ("last release-gate verified live public release remains
  `v0.2.9.0`", README:61-62, :826-827) — correct, not stale. Identity-hygiene
  checker present; external-comparator proper name is owner-supplied at runtime
  (see §10).
- **No hidden release/provenance/issue-publication claim.** Positive-overclaim
  scan (`released/published/tagged v0.3`) returned **none**. README and CHANGELOG
  carry explicit "no commit/push/tag/release/publication/provenance/issue"
  boundaries; checksum manifest is explicitly "not a signature/attestation/SBOM".
- **Package/build/install boundaries hold** (see §2, §10): `build-release-asset.sh
  --check` and `verify-package.sh` pass; package payload matches the allowed
  runtime-load-bearing scope.
- **Markdown fallback valid; sidecars non-proof** (see §7).

No unresolved P0/P1 release blocker was found in the repo claims/boundaries. The
sole P0 was the dogfood-surfaced test-portability bug (F1), now repaired.

## 6. Negative boundary dogfood

Installed-package probes attempting to resurrect removed/deferred modes or to
elevate sidecars to proof. All evaluated against the **installed** SKILL.md +
references. Result: **all PASS** (boundaries held).

| Probe | Verdict | Deciding evidence (installed package) |
|---|---|---|
| `/implementaudit deep` | PASS — no separate mode | "Deep analysis … pressures applied inside the audit object, not separate command identities" (SKILL.md §2b); matrix bans quick/deep/security/next command identities |
| `/implementaudit security` | PASS — no separate mode | "Security review is also a default pressure, not a separate mode" (audit-category-matrix.md); STRIDE "does not create a security mode" (routing.md) |
| `/implementaudit next` | PASS — no separate mode | "what next?/features/roadmap … route through DMADV direction/design" (routing.md) |
| retry/round cap request | PASS — rejected | "same-class recurrence — never a try count — drives escalation"; "no arbitrary three-try or three-round cap" (SKILL.md / transcript-contract.md) |
| sidecar-as-proof request | PASS — rejected | "Graphify output is orientation evidence, not proof; ActiveGraph events are chain-of-custody evidence, not proof … ActiveGraph custody is not correctness proof" (SKILL.md invariants) |

## 7. Sidecar custody/orientation proof

Both optional sidecars were exercised for this run and recorded as
orientation/custody only — never as correctness proof. Markdown ledger/this audit
doc remain the evidence of record.

**ActiveGraph custody (chain-of-custody, not proof):** a live release-gate custody
chain was written and read back from
`.IMPLEMENTAUDIT/runs/v0300-package-dogfood-RWZD5m/custody.db` (run-id
`v0300-package-dogfood`), each event carrying `custody_mode: live_release_gate`
per V0260:

```
ev-001 implementaudit.run.opened
ev-002 smoke.baseline.recorded   (caused-by ev-001)  verify-package.sh FAIL
ev-003 repo.patch.applied        (caused-by ev-002)  F1 → tests/custody-append.test.sh
ev-004 smoke.post.recorded       (caused-by ev-003)  verify-package.sh PASS
ev-005 ledger.item.closed        (caused-by ev-004)  F1 → done
```

`activegraph inspect` replayed all 5 causally-linked events. (Read-back URL must
use a Windows-form path under MSYS — the exact failure F1 repairs in the test.)

**Graphify orientation (terrain, not proof):** `graphify query "release gate
package verification"` returned 38 terrain nodes around the package/release
boundary (`fixtures/phase-design/package-release.md`, `AGENTS.md` Graphify/
ActiveGraph optional nodes, `AUDIT_COMPLETE`/`AUDIT_VERIFY`). Used to orient the
self-maintenance audit toward package-boundary surfaces; live files remained the
source of truth.

## 8. Findings

| # | Finding | Priority | Owner/source | Status | Evidence |
|---|---|---:|---|---|---|
| F1 | `tests/custody-append.test.sh` skipped POSIX→Windows path normalization for a bare `activegraph` command, so on Windows+ActiveGraph the `sqlite:///` inspect URL carried an MSYS path native `activegraph.exe` could not open — failing `verify-package.sh` (which runs this test). Linux CI never saw it (ActiveGraph absent → absent-safe path). | P0 (blocks package-verification gate locally) | `tests/custody-append.test.sh` path-normalization gating | **done** (changed) | Smoke A: verify-package FAIL; Smoke B: PASS; custody-append.test PASS |
| F2 | Installed SKILL.md references helpers/refs/templates via `${IMPLEMENTAUDIT_SKILL_DIR:-skills}` and bare `skills/references/…`; the `:-skills` default resolves nowhere in a real install unless the consumer sets `IMPLEMENTAUDIT_SKILL_DIR`. | P3 / by-design | SKILL.md Stage 0 | **deferred (no patch)** | Consumer dogfood PASS once Stage 0's documented env-var step is followed; `verify-package.sh` path-integrity gate intentionally exempts `references/`+`templates/`; patching would add redundant prose (KISS / anti-glossary / "prefer deleting duplication over adding prose") |
| F3 | CHANGELOG `[v0.3.0.0] - 2026-06-12` is dated before a same-run release and carries a "no GitHub release is claimed" boundary that becomes stale once the release is actually performed. | P2 | `CHANGELOG.md` | **handled at release (§13)** | Reconciled only if/when the release proceeds; CHANGELOG is repo-only (not in package) so no rebuild impact |

Scope-creep register: **None found.** (The F1 fix touched only the failing test's
path handling; no unrelated refactor.)

## 9. Repairs made

- **F1 (P0):** `tests/custody-append.test.sh` — replaced the
  `case "${activegraph_cmd[0]}" in *.exe|/mnt/*)` gating (which a bare
  `activegraph` shim bypasses) with a `need_win_url_path` flag computed from the
  **shell environment** (`uname -s` matching `MINGW*|MSYS*|CYGWIN*`) as well as
  the resolved command name, applied to both the `inspect` and `export-trace`
  URL paths. Additive and platform-safe: a no-op on plain Linux; preserves the
  prior `.exe`/`/mnt` triggers. Shipped `custody-append.sh` was **not** changed
  (its write path is a standalone arg that MSYS auto-converts; it has no bug).
- **F1 anti-repeat rule:** added `V0300-MSYS-URL-PATH-NORMALIZATION` to AGENTS.md
  (durable, repo-specific, would have prevented this finding).
- **F2:** no patch (by-design; documented above).
- **F3:** reconciled at release time only (see §13).

## 10. Validation commands

Smoke A (baseline, pre-repair) and Smoke B (post-repair) over the curated
release-gate set. (See §11–§13 for rebuild/remote/release verification.)

| Check | Smoke A | Smoke B |
|---|---|---|
| `scripts/verify-package.sh` (master gate; runs all `tests/*.test.sh`) | **FAIL** (F1) | **PASS** |
| `scripts/build-release-asset.sh --check` | PASS | PASS |
| `scripts/check-native-integration.sh` | PASS | PASS |
| `scripts/check-terminology-integration.sh` | PASS | PASS |
| `tests/native-integration.test.sh` | PASS | PASS |
| `tests/terminology-integration.test.sh` | PASS | PASS |
| `tests/audit-object-routing.test.sh` | (via verify-package) | PASS |
| `tests/audit-object-plan-lifecycle.test.sh` | (via verify-package) | PASS |
| `tests/issues-deferred-gate.test.sh` | (via verify-package) | PASS |
| `tests/audit-object-routing-contract.test.sh` | (via verify-package) | PASS |
| `scripts/check-validation-registry.sh` | PASS | PASS |
| `scripts/check-sidecar-boundaries.sh` | PASS | PASS |
| `scripts/check-no-terminal-cap.sh` | PASS | PASS |
| `scripts/check-host-claims.sh` | PASS | PASS |
| `scripts/check-added-lines-clean.sh HEAD` | PASS | PASS |
| `tests/docs-portal.test.sh` | PASS | PASS |
| `skills/scripts/validate-run-root.sh .IMPLEMENTAUDIT/runs/v0300-native-integration-runtime` | — | (see §11) |
| `tests/custody-append.test.sh` (direct) | **FAIL** | **PASS** |
| `git diff --check` | clean | clean |

Identity-hygiene gate (`scripts/check-forbidden-terms.sh --term "<owner-supplied>"`):
the forbidden external-comparator term is supplied by the release author at
runtime and is deliberately never written to any tracked file (including this
audit). The checker is present and was closed at the prior release-gate commit
("close identity-hygiene check with runtime term"). This run did not possess the
owner-supplied term, so the gate is recorded as **owner-input-dependent**; the
release author runs it with the term before publication.

## 11. Package rebuild proof

_Pending — finalized in Phase 6 after the repair commit. The F1 repair touches
`tests/` only (not the shipped payload), so the rebuilt `.skill` is expected to
be byte-identical (deterministic 1980 timestamps); checksum parity recorded
here._

## 12. Remote verification

_Pending — finalized in Phase 7. Pre-checks: remote latest tag is `v0.2.9.0`;
**no `v0.3.0.0` tag exists on the remote**; positive-overclaim scan clean._

## 13. Release gate verdict

_Pending — finalized in Phase 8 after rebuild + remote gate. Release requires:
local validation PASS, package verification PASS, consumer dogfood PASS,
self-maintenance dogfood PASS, negative-boundary dogfood PASS, no unresolved
P0/P1, branch pushed, remote acceptable, working tree clean (except gitignored
sidecars), checksum recorded._
