# v0.2.8.0 Adaptation Lane — Audit Ledger

Run root: `.IMPLEMENTAUDIT/runs/v028-adaptation-b3lU0C/`
Baseline ref: `963d48a`
Completed: 2026-06-08
Audit-fix round completed: 2026-06-08
ActiveGraph custody: 19 events (v28-001..v28-019; events v28-015..v28-019 are audit-fix round)

---

## Objective

Close G1-G7 comparator-advantage gaps identified by comparing v0.2.7.0 against a
read-only external staged-goal comparator package. Add governed casual-build
intake, bounded continuity preload, per-phase writeback, optional Polish & Harden
phase shape, a docs portal generator, and a GitHub Pages CI pipeline.

## Gap register

| ID | Gap | Status | Owner/source |
|---|---|---|---|
| G1 | Chooser table missing from onboarding (which invocation shape to use) | ADAPTED | `skills/references/goal-format.md` |
| G2 | Neutral identity hygiene — comparator language in comparator section | ADAPTED | `docs/portal/onboarding.md` §Compared |
| G3 | Casual build invocation — no governed intake mode | ADAPTED | `skills/SKILL.md` §2 + §2a + Stage 0; `skills/references/routing.md`; `skills/references/planning-depth.md` |
| G4 | Continuity preload — no bounded 5-source priority order | ADAPTED | `skills/SKILL.md` Stage 0; `skills/templates/PROTOCOL.md` Steps 14/15; `skills/templates/THINKING.md` |
| G5 | Per-phase continuity writeback — CONTINUITY_DECISION in phase loop | STRENGTHENED | `skills/templates/phase-goal.txt`; `skills/templates/PROTOCOL.md` Steps 14/15; `fixtures/continuity/safe-note-write.md`; `tests/continuity.test.sh` |
| G6 | Optional Polish & Harden phase shape | ADAPTED | `skills/references/phase-design.md` Rule P4-8; `skills/templates/phase-goal.txt`; `fixtures/phase-design/polish-harden.md` |
| G7 | Simpler "ship it" story — casual invocation examples, chooser, portal | ADAPTED | `docs/portal/onboarding.md` + `scripts/build-docs-portal.py` |

All 7 gaps: 7 ADAPTED (1 classified as STRENGTHENED — pre-existing base covered; v0.2.8.0 lane added marker, bounded options, custody path, and tests). 0 open.

## Phase-by-phase evidence

### Phase 1 — Gemba + Smoke A

- Smoke A: `bash scripts/verify-package.sh` — 6/6 smoke groups pass at baseline
- Graphify terrain: **reads of 3/5 pre-existing TEMP graph artifacts** from prior
  session (skills-semantic, scripts-AST, tests-AST); refs/ and templates/ graphs
  could not be re-extracted without API key — confirming no new LLM-assisted
  indexing occurred. Sidecar policy: Graphify is **canonical for
  self-maintenance runs** (mandatory since v0.2.4.5); terrain reads/queries are
  authorized self-maintenance operations, not a boundary crossing. Reads are
  orientation evidence only; live-file Gemba confirms all owner/source.
- ActiveGraph custody initialized: v28-001..v28-003 (run.opened, smoke-a.done,
  terrain.queried)
- Andon RESOLVED: sidecar venv rebuilt from prior session; 3/5 graphs sufficient
  for orientation
- Sidecar authorization boundary: no new Graphify indexing (no new graph was
  created or refreshed); terrain reads used pre-existing artifacts produced in a
  prior authorized session.

### Phase 2 — SKILL.md governed casual-build intake (G3)

- `skills/SKILL.md` §2: added 4th invocation shape bullet (governed casual-build intake)
- `skills/SKILL.md` §2 canonical audit terminology: added governed casual-build intake constructs bullet
- `skills/SKILL.md` §2a: added governed casual-build intake paragraph in invocation boundary
- `skills/SKILL.md` Stage 0: added Bounded continuity preload block (5-source priority order + IMPLEMENTAUDIT_CONTINUITY_SAVED marker)
- `scripts/check-planner-stages.sh`: 2 require_in_file checks for casual-build intake
- `scripts/check-routing.sh`: added "governed casual-build intake" to routing definitions check
- Evidence: `bash scripts/verify-package.sh` → ok (custody v28-004)

### Phase 3 — Continuity preload + writeback (G4)

- `skills/templates/PROTOCOL.md` Steps 14/15: CONTINUITY_DECISION options table (5 options), override-prevention statement, IMPLEMENTAUDIT_CONTINUITY_SAVED reference
- `skills/templates/THINKING.md`: invocation shape line updated with 4th mode
- `tests/continuity.test.sh`: extended to 34/34 (was 25/25; +9 checks)
- `fixtures/continuity/safe-note-write.md`: CONTINUITY_DECISION updated to run-local applied-context note; IMPLEMENTAUDIT_CONTINUITY_SAVED marker block added with all 6 required fields
- Evidence: `bash tests/continuity.test.sh` → 34/34 ok (custody v28-005)

### Phase 4 — Reference docs + templates (G1, G4, G6)

- `skills/references/goal-format.md`: chooser table (4 rows), easy invocation examples, IMPLEMENTAUDIT_CONTINUITY_SAVED format block, Polish & Harden note
- `skills/references/routing.md`: Governed casual-build intake definition + 5-step process; updated Positive Identity section
- `skills/references/planning-depth.md`: governed casual-build intake subsection; casual-build planning bar definition
- `skills/references/phase-design.md`: Rule P4-8 — Polish & Harden (optional terminal phase shape; default-recommended conditions; no new features; covers cleanliness/identity hygiene/generated-artifact freshness/proof-boundary wording)
- `skills/templates/phase-goal.txt`: Polish & Harden variant comment block added at end
- Evidence: `bash scripts/verify-package.sh` → ok (custody v28-006)

### Phase 5 — Tests + fixtures (G3, G6)

- `fixtures/casual-build/accepted-intent.md`: documents casual-build intent accepted and normalized into audit object; Stage 1 intake; synthesized tdqyq-audit-object
- `fixtures/casual-build/rejected-intent.md`: 4 rejection cases (empty, unsafe, non-repo, unbounded)
- `fixtures/phase-design/polish-harden.md`: two variants (included with rationale; skipped with rationale)
- Evidence: all tests including new continuity checks pass (custody v28-007..v28-009)

### Phase 6 — Docs portal generator + check scripts (G7)

- `docs/portal/onboarding.md`: 10 onboarding sections (What is; When to use; When not; Chooser table; 3 invocation modes; Compared with generic; Graphify/ActiveGraph; Completion; Install; Easy examples)
- `scripts/build-docs-portal.py`: stdlib-only generator; dark theme; skip link; sidebar nav; IntersectionObserver; 9 required section anchors; no external deps; `rough_draft_used: false`
- `scripts/check-docs-portal.py`: 12-check validator (index.html, metadata, sha256s, no file:///,  no Windows paths, nav anchors unique+ordered, 9 required sections, no unsupported claims, skip link, semantic headings)
- `tests/docs-portal.test.sh`: 26-check test suite
- `scripts/check-added-lines-clean.sh`: [audit-fix reversed — see §Audit-fix round below]
  `build-docs-portal.py` and `check-docs-portal.py` user-facing CLI output now uses
  `sys.stdout.write`/`sys.stderr.write`; no `*.py` exclusion in `is_skipped_path()`
- `.gitignore`: `_site/` added; `dist/` already covered
- Evidence: `bash tests/docs-portal.test.sh` → 26/26 ok; `bash scripts/verify-package.sh` → ok (custody v28-010)

### Phase 7 — GitHub Pages CI + validate.yml

- `.github/workflows/pages.yml`: build+validate+deploy pipeline; OIDC permissions; concurrency guard; OWNER DECISION note (Pages source must be set to "GitHub Actions")
- `.github/workflows/validate.yml`: `bash tests/docs-portal.test.sh` added to test suite
- Evidence: `bash -n` YAML evidence replaced in audit-fix (see §Audit-fix round);
  `verify-package.sh` ok (custody v28-011)

## Comparator language check

`docs/portal/onboarding.md` §"Compared with a generic staged-goal runner":

> Generic staged-goal tools often optimize for low-friction autonomous build execution.
> ...IMPLEMENTAUDIT now absorbs the useful parts through governed casual-build intake,
> bounded continuity preload, per-phase continuity writeback, and optional polish/harden phases.
> The difference is that IMPLEMENTAUDIT does not let those conveniences bypass...
> No all-domain obsolescence claim is made. Each tool has its own scope and users.

Neutral framing confirmed. No comparator proper name in any tracked surface.

## Identity hygiene

Formal forbidden-term check executed (audit-fix round, 2026-06-08) using
`scripts/check-forbidden-terms.sh` with the external comparator proper name,
slash command, marker names (including phase markers and memory writeback marker),
and artifact paths supplied at runtime only — not embedded in this file or any
tracked source.

Results:

| Scope | Files scanned | Forbidden term occurrences |
|---|---|---|
| `skills/` | 22 | 0 |
| `scripts/` | 17 | 0 |
| `.github/` | 2 | 0 |
| `docs/` | 15 | 0 |
| `README.md` | 1 | 0 |
| `AGENTS.md` | 1 | 0 |
| `CHANGELOG.md` | 1 | 0 |

Full tracked-file scan (all git-tracked files, default scope) found 13 occurrences of
the comparator memory-writeback marker in `tests/continuity.test.sh` and
`fixtures/continuity/` only. These are all meta-references (the continuity test
enforces boundary hygiene by grep-checking native surfaces for the marker's absence;
the fixtures illustrate boundary concepts). Zero occurrences in any native authoring
surface. All references in tracked files use: "external staged-goal comparator",
"generic staged-goal runner", or "comparator".

## Smoke B (final state — after audit-fix round)

```
bash tests/docs-portal.test.sh           → 26/26 ok
bash scripts/verify-package.sh           → ok (all suites pass)
bash tests/continuity.test.sh            → 34/34 ok
bash tests/routing.test.sh               → ok
bash tests/phase-validation.test.sh      → ok
bash tests/sidecars.test.sh              → ok
bash tests/lean-discipline.test.sh       → ok
bash scripts/check-planner-stages.sh     → ok
bash scripts/check-routing.sh            → ok
python scripts/check-workflow-structure.py \
  .github/workflows/pages.yml \
  .github/workflows/validate.yml         → ok (2 files pass structural checks)
                                            NOTE: full YAML schema validation pending CI
scripts/check-forbidden-terms.sh         → ok (native surfaces: 0 occurrences)
git diff --check                         → ok
```

## Remaining risk

- GitHub Pages deployment unverified: pages.yml build+validate will run in CI,
  but the deploy job requires the repository owner to set Pages source to
  "GitHub Actions" in Settings → Pages. OWNER DECISION required.
- Graphify terrain refs/ and templates/ graphs not re-extracted (no API key in
  shell session). Owner/source confirmed via live-file Gemba. This is not a
  blocking risk for this lane. Sidecar policy: Graphify reads/queries are
  authorized self-maintenance operations (canonical since v0.2.4.5); no new
  indexing was performed.
- GitHub Actions YAML schema validation pending CI (structural check passed; full
  validation requires workflow to run in GitHub Actions).
- No commit, push, tag, release, release asset upload, marketplace update,
  provenance claim, tool install, new Graphify indexing, or ActiveGraph export was
  performed in this lane.

## Audit-fix round (2026-06-08)

Five closure issues identified before accepting AUDIT_COMPLETE; resolved in one
bounded audit-fix round. No new scope added.

| Item | Issue | Resolution |
|---|---|---|
| AF-1 | Graphify authorization boundary — "no Graphify indexing" vs "terrain extracted 3/5 corpora" unreconciled | Phase 1 evidence updated: terrain reads were of pre-existing TEMP artifacts (authorized self-maintenance); no new indexing; 2/5 graphs not re-extractable without API key confirms no new LLM-assisted indexing |
| AF-2 | G5 status was ALREADY COVERED; should be STRENGTHENED | Gap table, summary line, and terminal closure updated to STRENGTHENED with full evidence list |
| AF-3 | Broad `*.py` print() exemption in check-added-lines-clean.sh is a durable weakening | Reversed: `*.py` exclusion removed; `build-docs-portal.py` and `check-docs-portal.py` now use `sys.stdout.write`/`sys.stderr.write`; AGENTS.md rule updated to V0280-PY-USE-STDOUT-WRITE |
| AF-4 | `bash -n` was claimed as YAML workflow validation evidence | Corrected: `scripts/check-workflow-structure.py` (stdlib-only structural checker) created; explicitly notes full YAML schema validation is pending CI |
| AF-5 | Identity hygiene — formal forbidden-term check not run | Executed: `check-forbidden-terms.sh` with comparator proper name, slash command, marker names, memory marker, and artifact paths supplied at runtime; all native surfaces clean (0 occurrences); see §Identity hygiene |

ActiveGraph custody events added: v28-015 (audit-fix.opened), v28-016 (items 3+4 fixed),
v28-017 (item-5 forbidden-terms ok), v28-018 (items 1+2 fixed),
v28-019 (audit-fix.complete).

## Terminal closure

| G1 | ADAPTED: chooser table in goal-format.md + onboarding.md |
| G2 | ADAPTED: neutral comparator language in onboarding.md; formal forbidden-term check passed (all native surfaces, 2026-06-08) |
| G3 | ADAPTED: governed casual-build intake in SKILL.md §2/§2a/Stage 0, routing.md, planning-depth.md, fixtures |
| G4 | ADAPTED: bounded continuity preload in SKILL.md Stage 0, PROTOCOL.md, THINKING.md, continuity tests 34/34 |
| G5 | STRENGTHENED: v0.2.6.0 PROTOCOL loop base covered; v0.2.8.0 added IMPLEMENTAUDIT_CONTINUITY_SAVED marker (6 required fields), bounded preload/writeback options table (5 options), ActiveGraph custody path, Graphify terrain-update request, 34-check continuity test suite (+9 from v0.2.6.0), and new fixtures |
| G6 | ADAPTED: Polish & Harden rule P4-8 in phase-design.md, phase-goal.txt variant, fixtures |
| G7 | ADAPTED: docs portal generator, check script, 26/26 test suite, GitHub Pages CI, easy invocation examples |

All 7 ledger items terminally closed. 0 open.
