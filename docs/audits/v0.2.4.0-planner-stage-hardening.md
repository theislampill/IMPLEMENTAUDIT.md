# v0.2.4.0 Planner Stage Hardening

This ledger records the v0.2.4.0 implementation lane for native planner-stage
behavior in IMPLEMENTAUDIT. It is audit evidence, not a release log, raw smoke
dump, provenance manifest, or substitute for live validation.

## Input basis

- Owner goal: construct v0.2.4.0 from the planner-stage hardening goal.
- External audit input: `stages-audit.md`, including the v0.2.4.0 supplement.
- External comparator archive: read-only staged-goal package evidence.
- Source of truth: current repo checkout, `AGENTS.md`, and canonical
  `skills/SKILL.md`.

The external comparator is used only to classify reusable architecture patterns.
No external package name, marker namespace, artifact directory, autonomous-runner
identity, or completion semantics are imported into repo-facing files.

## Findings ledger

| Finding | Owner/source | Root cause | Countermeasure | Status |
|---|---|---|---|---|
| Planner lifecycle existed as distributed guidance but was not first-class native behavior. | `skills/SKILL.md` | Goal synthesis, phased closure, and pre-flight stages were spread across references/templates. | Added native Stage 0-7 planner contract with invocation boundary, Gemba, deep-think evidence, phase decomposition, self-critique, pre-flight, and handoff rules. | changed |
| Runtime planning evidence named `.IMPLEMENTAUDIT/THINKING.md` but package lacked a template and validation. | `skills/templates/THINKING.md`, `scripts/check-planner-stages.sh` | References could point to a runtime artifact that was not shipped or checker-owned. | Added `THINKING.md` template and validation coverage across package, release asset, install-copy smoke, and CI. | changed |
| Stage 6 / Stage 6.5 review and pre-flight were not enforced as a checker-owned contract. | `skills/SKILL.md`, `skills/references/phase-design.md`, `skills/references/transcript-contract.md` | Self-critique and pre-flight output could be skipped or weakened under context pressure. | Added Stage 6 self-critique, Stage 6.5 `PREFLIGHT_GREEN` / `PREFLIGHT_RED`, failure classification, and abnormal-command Andon rule. | changed |
| Stage 7 handoff needed to preserve embedded-governance boundaries. | `skills/SKILL.md`, `skills/references/goal-format.md`, `skills/templates/PROTOCOL.md`, `skills/templates/phase-goal.txt` | A generated handoff could be mistaken for automatic dispatch or a second goal inside an active goal. | Added one-handoff-only rule, embedded no-second-goal boundary, phase marker expectations, and final audit before completion. | changed |
| v0.2.4.0 metadata and package evidence needed to match the new behavior. | `README.md`, `CHANGELOG.md`, `AGENTS.md`, `.claude-plugin/plugin.json`, package scripts/tests | New planner behavior would otherwise ship without milestone, package, release-asset, or CI coverage. | Updated milestone/version text, package validators, release asset tests, install-copy smoke, and GitHub Actions. | changed |
| External comparator identity and generic runner framing must remain absent. | `scripts/check-planner-stages.sh`, `scripts/verify-package.sh`, `scripts/check-added-lines-clean.sh` | Useful planner architecture came from external evidence, but repo identity and proof model must remain IMPLEMENTAUDIT-specific. | Added/retained scans and source text that adapt concepts without importing external identity, path names, or generic autonomous-runner claims. | changed |
| Forbidden external-name protection was itself storing source-embedded fragments and marker checks. | `scripts/check-forbidden-terms.sh`, validation scripts, checker tests | The checker tried to prevent leakage by reconstructing sensitive source terms in tracked code. | Replaced project-specific checks with generic externally supplied forbidden-term validation and neutral sentinel tests. | changed |
| Runtime docs blurred audit object state with auditing actions. | `skills/SKILL.md`, runtime references/templates, planner checker, transcript fixture | "Audit" was used for both the evidence-bearing record and the verification action without a canonical runtime distinction. | Added audit-object/auditing-action terminology to the runtime contract, templates, checker coverage, and transcript skeleton. | changed |
| `.skill` asset boundary included repo-maintenance evidence that was not installed-runtime load-bearing. | `scripts/build-release-asset.sh`, release-asset tests, README, AGENTS.md | The artifact allowlist still reflected an earlier broad package shape that included audit ledgers and README-generation sources. | Tightened the release asset to `skills/` plus `.claude-plugin/` metadata and added checks rejecting repo-only top-level paths. | changed |
| Release-affecting work needed a dogfooded double-audit sequence, not inspect-then-mutate. | `skills/SKILL.md`, runtime references/templates, planner checker, this ledger | The noun/action distinction needed to govern release, package, and claim hygiene in the runtime. | Added double-audit runtime rules and applied them to this v0.2.4.0 cleanup ledger. | changed |

## User-facing onboarding and install pipeline

| Finding | Classification | Owner/source | Countermeasure | Evidence status |
|---|---|---|---|---|
| README navigation and install clarity needed to be as practical as the planner contract. | changed | `README.md`, `scripts/check-readme-toc.sh` | README keeps a compact Contents list and separates Codex, Claude Code, release asset, update, and evidence-boundary paths. | done: `bash scripts/check-readme-toc.sh`, `bash scripts/verify-package.sh` |
| Codex manual install path needed release-asset proof, not a vague copy-only claim. | changed | `scripts/install-codex-from-release.sh`, install smoke tests | Added a release-asset installer path that installs into an overrideable Codex home and validates installed skill shape/version. | done: `bash tests/release-asset-install.test.sh`, `bash scripts/verify-package.sh` |
| Claude Code/plugin install wording must stay documented but not oververified. | changed | `README.md`, `scripts/check-host-claims.sh` | README describes documented plugin flow without claiming marketplace or host install verification unless separately tested. | done: `bash scripts/check-host-claims.sh`, `bash scripts/verify-package.sh` |
| SSH-vs-HTTPS caveat is useful only for clone/plugin flows where it applies. | changed | `README.md` | README notes HTTPS is simplest for public clone/plugin setup, while SSH requires working GitHub SSH auth. | done: manual README inspection plus `bash scripts/verify-package.sh` |
| Upgrade/reinstall path must not imply passive auto-update. | changed | `README.md`, `CHANGELOG.md` | Docs state Codex manual installs do not auto-update and must repeat the install/update step after release. | done: `bash scripts/check-host-claims.sh`, `bash scripts/verify-package.sh` |
| Checksum-manifest verification needed a negative stale-checksum smoke. | changed | `tests/release-asset-install.test.sh` | Added stale checksum mutation case that must fail before install is trusted. | done: `bash tests/release-asset-install.test.sh`, `bash scripts/verify-package.sh` |
| One `/goal`, not a chain, needed a native explanation. | changed | `README.md`, `skills/SKILL.md` | README explains the bounded end-state plus `.IMPLEMENTAUDIT/*` phase files and terminal markers instead of a fragile command chain. | done: `bash scripts/check-planner-stages.sh`, `bash scripts/verify-package.sh` |
| Public GitHub release download smoke can only be claimed after the release exists. | changed | Release gate final audit | This lane tested local release-asset-to-Codex install before release and then ran strict public tag-based download/checksum/install into a temporary Codex-style skill home after the release existed. | done: `scripts/install-codex-from-release.sh --tag v0.2.4.0` smoke passed |

## Release-blocking cleanup supplement

| Finding | Classification | Owner/source | Countermeasure | Evidence status |
|---|---|---|---|---|
| Source-embedded forbidden external-name fragments in checker logic made the guard itself violate the repo hygiene rule. | changed | `scripts/check-forbidden-terms.sh`, `scripts/check-host-claims.sh`, `scripts/check-planner-stages.sh`, `scripts/check-added-lines-clean.sh` | Removed embedded fragments and project-specific marker checks; added externally supplied forbidden-term scanning with neutral sentinel tests. | done locally: tracked source and rebuilt local asset scans passed; uploaded asset and source archive scans remain release-gate evidence |
| Audit noun/action ambiguity could make completion look like a performed pass instead of an audit-object state transition. | changed | `skills/SKILL.md`, `skills/references/*.md`, `skills/templates/*`, `fixtures/simple-audit/EXPECTED-TRANSCRIPT-SKELETON.md` | Defined `tdqyq-audit-object` and `ydqyq-audit-action`, then threaded audit object/action semantics through runtime contracts, templates, fixtures, and validators. | done: planner-stage checker and package verifier passed |
| README explained the audit noun/action distinction less clearly than the canonical runtime contract. | changed | `README.md`, `docs/diagrams/invocation-modes.mmd` | Added a compact `How IMPLEMENTAUDIT audits` section and made the invocation-mode diagram show `tdqyq-audit-object` ownership and `ydqyq-audit-action` behavior in direct, embedded, and goal-synthesis modes. | done: README generated-region check, ToC check, host-claim check, and package verifier passed |
| Release-affecting cleanup needed double-audit dogfood. | changed | this ledger, runtime contract, package/release validators | First auditing pass produced this cleanup supplement; second pass patched checker/runtime/package owners against it; final pass verified terminal state, asset rebuild, checksum, source archive, and public release state. | done: double-dogfood pass reached terminal audit-object closure after release readback |
| Installer accepted required-entry archives without enforcing the new minimal top-level package boundary. | changed | `scripts/install-codex-from-release.sh`, `tests/release-asset-install.test.sh` | Added installer top-level allowlist and overbroad-but-checksummed archive negative smoke. | done: release-asset install smoke and full package verifier passed |
| First-time user onboarding load remains a future improvement but should not widen this cleanup. | deferred | `AGENTS.md` | Added a future-work note for a dedicated quickstart/onboarding docs page without implementing that page in this release-blocking cleanup. | done: AGENTS_UPDATE_DECISION source patch |

## Package-boundary manifest classification

| Asset path/group | Classification | Included? | Reason |
|---|---|---:|---|
| `skills/SKILL.md` | runtime core | yes | Canonical installed skill entry and behavior source. |
| `skills/references/` | canonical reference | yes | Progressive runtime contract details loaded by the skill when needed. |
| `skills/templates/` | runtime template | yes | Copied into `.IMPLEMENTAUDIT/` runtime state during phase planning. |
| `skills/scripts/` | validator/helper required by installed runtime | yes | Runtime helpers for environment/repo detection, repo-state comparison, and phase/spec validation. |
| `.claude-plugin/` metadata | metadata required by host/package contract | yes | Declares plugin/package identity and skill path for host/package consumers. |
| `README.md`, `CHANGELOG.md`, `AGENTS.md`, `CLAUDE.md`, `CONTRIBUTING.md` | repo-maintenance docs | no | Public and maintainer docs stay repo-side; installed runtime contract is in `skills/`. |
| `docs/diagrams/` | README generation source | no | Load-bearing for repo README generation, not installed skill execution. |
| `docs/audits/` | audit/release evidence | no | Repo-side dogfood and release evidence; not needed in installed runtime payload. |
| `fixtures/`, `tests/`, root `scripts/`, `.github/` | repo validation/CI surfaces | no | Used to validate the repo and release asset before publication, not installed runtime payload. |
| `dist/`, `.IMPLEMENTAUDIT/`, Git metadata, Graphify/ActiveGraph outputs | generated/local/debris surfaces | no | Must never be packaged. |

## Release-asset delta audit

Read-only subagent `019e9b10-0c20-71d0-bc55-07dfae356529` compared public
`IMPLEMENTAUDIT.skill` assets for `v0.2.2.0`, `v0.2.3.0`, and `v0.2.4.0`.
The verdict was `smaller-by-design`: `v0.2.4.0` is smaller because the package
now contains only the installed runtime payload, `skills/` plus
`.claude-plugin/` metadata.

| Tag | Asset bytes | ZIP entries | Extracted files | Top-level paths | Checksum |
|---|---:|---:|---:|---|---|
| `v0.2.2.0` | 166,405 | 25 | 25 | `.claude-plugin`, `CHANGELOG.md`, `IMPLEMENTAUDIT.md`, `README.md`, `docs`, `skills` | matches |
| `v0.2.3.0` | 186,507 | 29 | 29 | `.claude-plugin`, `CHANGELOG.md`, `README.md`, `docs`, `skills` | matches |
| `v0.2.4.0` | 113,775 | 22 | 22 | `.claude-plugin`, `skills` | matches |

| Removed from older assets in `v0.2.4.0` | Classification | Reason |
|---|---|---|
| `CHANGELOG.md` | repo-only-history | Changelog remains repo-side release history, not installed runtime behavior. |
| `README.md` | intended-package-minimality | Public onboarding remains repo-side; installed runtime contract is under `skills/`. |
| Root `IMPLEMENTAUDIT.md` from `v0.2.2.0` | intended-package-minimality | Root behavior file was intentionally removed; canonical runtime is `skills/SKILL.md`. |
| `docs/audits/*` | repo-only-history | Dogfood/release ledgers are evidence records, not installed runtime payload. |
| `docs/diagrams/*.mmd` | intended-package-minimality | README diagram sources are repo-maintenance sources, not installed runtime behavior. |

| Required installed-runtime surface | `v0.2.4.0` public asset result |
|---|---|
| `skills/SKILL.md` | present |
| `skills/references/*` | seven references present, including `repo-state-comparison.md` |
| `skills/templates/*` | six templates present, including `THINKING.md` |
| `skills/scripts/*` | six runtime helpers present |
| `.claude-plugin/*` | `plugin.json` and `marketplace.json` present |
| Plugin metadata | `name=implementaudit`, `version=0.2.4`, `skills=./skills/` |
| README / CHANGELOG / LICENSE | absent by package design; no repo evidence says they are required inside `.skill` |

Public `v0.2.4.0` matched the current builder manifest at the time of the
audit: 22 entries, 113,775 bytes, no missing files, no extra files. Because the
current Mermaid/README Andon changes do not alter packaged `skills/` or
`.claude-plugin/` payload, the `.skill` content is expected to stay byte-identical
after final source retag. Final release readback verifies the public asset,
checksum, source archive, and tag after the closing commit.

## External-reference review closure

| Review | Evidence | Findings classification | Status |
|---|---|---|---|
| Read-only review subagent | Subagent `019e9af3-2b44-74f0-bfec-b177cf435a67` inspected current repo source, release asset/checksum, public release metadata, and external archive patterns read-only. | Release-blocking: stale public/local asset at time of review, checksum tied to stale asset, installer overbroad-archive gap, release-proof wording pending repair; already covered: runtime semantics, README generation, optional sidecars. | done: installer gap patched and final release repair readback passed |
| Adversarial review subagent | Subagent `019e9af3-6db8-7941-beef-abac2b951e13` challenged runtime semantics, double dogfood, package minimality, generated README, install proof, stale checksum, sidecar optionality, forbidden-term hygiene, public source/assets, and marker closure. | Release-blocking: public tag/source/assets/checksum still pointed to old state until same-version repair; already covered: local runtime/checker/package tests. | done: same-version release repair readback passed |

## Safety boundaries

- Root `IMPLEMENTAUDIT.md` remains intentionally absent.
- Canonical behavior source remains `skills/SKILL.md`.
- Audit inputs named `AUDIT.md` remain valid run artifacts.
- Graphify remains optional terrain/orientation only.
- ActiveGraph remains optional custody/event evidence only.
- No Graphify install, indexing, or graph mutation is performed by this lane.
- No ActiveGraph quickstart, config, event store, or export is performed by this
  lane.
- No release, tag, publication, marketplace verification, install verification,
  license claim, signature, attestation, SBOM, or provenance claim is made until
  the explicit release gate performs and verifies it.

## Smoke A

Before source mutation, the repo was clean against `origin/main` with only
ignored `dist/` and `.IMPLEMENTAUDIT/` artifacts present. Baseline package
verification and release-asset check passed.

## Validation Andons

| Andon | Trigger | Containment / fix | Closure proof |
|---|---|---|---|
| ANDON-20260606-V024-PLANNER-CHECKER-BACKTICK-QUOTE | `bash scripts/check-planner-stages.sh` initially executed Markdown backticks in expected strings and reported `Stage 5 - Write  runtime artifacts` missing. | Patched `scripts/check-planner-stages.sh` to single-quote expected strings containing backticks. | `bash scripts/check-planner-stages.sh` passed; `bash tests/planner-stages.test.sh` passed. |
| ANDON-20260606-V024-HOST-CLAIM-CHECKER-NEGATIVE-CONTEXT | `bash scripts/check-host-claims.sh` overflagged negative boundary wording and its own blocked-phrase list. | Excluded the checker source from its own scan, added negative-context phrases, and kept sensitive terms on the same line as the negative claim where needed. | `bash scripts/check-host-claims.sh` passed; `bash scripts/verify-package.sh` passed. |
| ANDON-20260606-V024-INSTALLER-CLEANLINESS-PRINT | `bash scripts/check-added-lines-clean.sh HEAD` flagged Python `print()` calls in the new installer. | Replaced `print()` with explicit `sys.stdout.write()` success output. | `bash scripts/check-added-lines-clean.sh HEAD` passed; `bash tests/release-asset-install.test.sh` passed; `bash scripts/verify-package.sh` passed. |
| ANDON-20260606-V024-POWERSHELL-POSIX-TEST | Root behavior-file absence check was first invoked as POSIX `test ! -e IMPLEMENTAUDIT.md` under PowerShell, where `test` is not a cmdlet. | Stopped counting that failed invocation and reran the check through Bash. | `bash -lc 'test ! -e IMPLEMENTAUDIT.md && echo root-implementaudit-absent: ok'` passed. |
| ANDON-20260606-V024-STAGED-EOF-WHITESPACE | `git diff --cached --check` found extra blank lines at EOF in three new files. | Trimmed EOF blanks in `skills/templates/THINKING.md`, `tests/planner-stages.test.sh`, and `tests/release-asset-install.test.sh`. | `git diff --cached --check` passed after restaging. |
| ANDON-20260606-V024-PUBLIC-SMOKE-NONSTRICT-SHELL | A first public release install smoke command produced misleading output because it was not run through a strict Bash script and a quoting/substitution issue attempted to install under `/codex home`. | Stopped counting the ambiguous result and reran the public install smoke with `set -euo pipefail`, temp Codex home, and explicit tag. | Strict rerun passed: `install-codex-from-release: sha256 c5a0eb21e7210fde065cbb9cb8edcd82cee158b045230595d716a14b7ecb4830`; installed `SKILL.md` existed. |
| ANDON-20260606-V024-READONLY-REVIEW-DOC-GAPS | Read-only reviewer found stale release-state wording, non-self-contained README checksum validation snippet, and changelog wording that implied v0.2.4.0 added an older README audit ledger. | Patched README checksum commands, changelog lineage wording, ledger release-state evidence, and package canaries. | `bash scripts/verify-package.sh` and focused doc checks passed after patch. |
| ANDON-20260606-V024-LATE-FINAL-AUDIT-STEER | The v0.2.4.0 branch push, tag, and release were created before the late final-audit steer requiring explicit external-artifact closure and reviewer evidence was fully accounted for. | Paused further release mutation, ran read-only/adversarial subagents, patched the required doc/evidence gaps, recorded dogfood/review evidence, and prepared to repair v0.2.4.0 release/source/assets to the final corrected commit. | Closed only after follow-up commit, CI, tag/source readback, asset/checksum rebuild, release metadata update, and strict public install smoke all pass. |
| ANDON-20260606-V024-FORBIDDEN-TERM-CHECKER-SOURCE-LEAK | A tracked checker/source file indirectly encoded a forbidden external project name by assembling fragments. | Removed source-embedded fragments and marker checks; replaced them with generic externally supplied forbidden-term checking and neutral sentinel canaries. | Closure requires clean tracked-source scan, release-asset scan, source-archive scan if applicable, and generic checker positive/negative tests. |
| ANDON-20260606-V024-FORBIDDEN-CHECKER-CLEANLINESS-PRINT | `bash scripts/verify-package.sh` failed because `bash scripts/check-added-lines-clean.sh HEAD` flagged Python `print()` calls in the new forbidden-term checker. | Patched `scripts/check-forbidden-terms.sh` to use explicit stdout writes instead of `print()`. | Closed: `bash scripts/check-added-lines-clean.sh HEAD` and `bash scripts/verify-package.sh` passed on rerun. |
| ANDON-20260606-V024-EXTERNAL-TERM-ENV-PASSING | A tracked-source forbidden-term scan was first invoked with the term set in PowerShell, but the Bash checker reported no terms supplied. | Stopped counting the failed invocation and reran with the externally supplied term through the checker CLI. | Closed: tracked source and rebuilt local asset scans passed with externally supplied CLI term. |
| ANDON-20260606-V024-ASSET-SCAN-SHELL-MODE | Release-asset scan/list commands were first sent as POSIX syntax to PowerShell, so `mktemp` and `unzip` were not recognized; Bash also lacked `unzip`. | Stopped counting the failed invocations and reran asset scan/list with Python `zipfile`. | Closed: Python `zipfile` content list was captured and rebuilt local asset scan passed. |
| ANDON-20260606-V024-OVERBROAD-ASSET-TEST-PYTHON-CMD | `bash tests/release-asset-install.test.sh` failed because the new overbroad-archive negative canary invoked bare `python`, which was not available in that Bash environment. | Patched the test to use the repo-standard `python` / `python3` / `py -3` command detection. | Closed: `bash tests/release-asset-install.test.sh` and `bash scripts/verify-package.sh` passed on rerun. |
| ANDON-20260606-V024-GITHUB-MERMAID-INIT-RENDER | GitHub README screenshots showed generated Mermaid blocks failing with `Unable to render rich display` and exposing raw `%%{init...}` diagram source. | Removed Mermaid init/theme/html-label directives from all README diagram sources, regenerated README only through `scripts/generate-readme-diagrams.sh`, and hardened the generator check to reject those directives in diagram sources. | Closed after `bash scripts/generate-readme-diagrams.sh --check`, `bash scripts/verify-package.sh`, `git diff --check`, and a source/readme scan confirming no `%%{init`, `themeCSS`, or `htmlLabels` remains in `README.md` or `docs/diagrams/`. |
| ANDON-20260606-V024-README-TOC-RENAMED-AUDIT-ANCHOR | After clarifying that README should not imply a required root `AUDIT.md` layout, `bash scripts/check-readme-toc.sh` still required the old `how-auditmd-drives-a-run` anchor and failed. | Updated `scripts/check-readme-toc.sh` to require the new `how-an-audit-input-drives-a-run` anchor. | Closed after `bash scripts/check-readme-toc.sh` and `bash scripts/verify-package.sh` passed on rerun. |

These were source/checker calibration failures found before commit, push, tag,
release, or provenance publication. No failed command output is counted as pass
evidence.

### Mermaid GitHub render Andon detail

| Failing README block | Source owner | Classification | Countermeasure |
|---|---|---|---|
| `implementaudit-diagram:tooling-architecture` | `docs/diagrams/tooling-architecture.mmd` | Renderer limitation / unsupported Mermaid init-theme syntax in GitHub README. Not generated README drift. | Removed `%%{init...}` and custom theme directives from the source, then regenerated README. |
| `implementaudit-diagram:invocation-modes` | `docs/diagrams/invocation-modes.mmd` | Renderer limitation / unsupported Mermaid init-theme syntax in GitHub README; semantic diagram also needed clearer audit object/action labels. Not generated README drift. | Removed init/theme directives, added `tdqyq-audit-object` and `ydqyq-audit-action` nodes, preserved no nested `/goal` and terminal closure semantics, then regenerated README. |
| `implementaudit-diagram:execution-spine` | `docs/diagrams/execution-spine.mmd` | Renderer limitation / unsupported Mermaid init-theme syntax in GitHub README. Not generated README drift. | Removed `%%{init...}` and custom theme directives from the source, preserved optional Graphify/ActiveGraph and release-gate semantics, then regenerated README. |

The fix favors simpler standard Mermaid over prettier renderer-dependent
styling. GitHub visual readback remains part of the final public readback after
the commit is pushed.

## Smoke B

Passing validation evidence:

```text
bash scripts/generate-readme-diagrams.sh --check
bash scripts/check-readme-toc.sh
bash scripts/check-planner-stages.sh
bash tests/planner-stages.test.sh
bash tests/release-asset-install.test.sh
bash tests/forbidden-terms.test.sh
bash scripts/check-host-claims.sh
bash scripts/check-added-lines-clean.sh HEAD
bash scripts/verify-package.sh
bash scripts/generate-readme-diagrams.sh --check
! rg -n "%%\{init|themeCSS|htmlLabels" README.md docs/diagrams
```

`bash scripts/verify-package.sh` also ran marker-order, routing, release-asset,
install-copy, repo-state, audit-spec, added-lines, release-asset extraction, and
`git diff --check` coverage.

## External artifact closure

| External artifact | Lines/headings checked | Closure |
|---|---|---|
| `C:\workspace\ai\adhlbs\goal-v0.2.4.0.md` | Lines 1-168 | done: native Stage 0-7 planner behavior, README/generated boundaries, optional Graphify/ActiveGraph boundaries, layout decision, validators, explicit-path staging, commit, push, tag, release, assets, checksum manifest, and final audit conditions were checked against repo/release evidence. |
| `C:\workspace\ai\adhlbs\stages-audit.md` | `## Post-audit supplement for v0.2.4.0` lines 207-248; `## User-facing onboarding and install-pipeline supplement` lines 250-294 | done/changed/rejected/unverified classifications were appended; repo implementation closed the changed items; rejected items remain intentionally rejected by truth boundaries. |

## Dogfood / review evidence

| Requirement | Evidence | Status |
|---|---|---|
| Stage 0-7 planner semantics discoverable from packaged skill | Installed `dist/IMPLEMENTAUDIT.skill` into a temp Codex home and grepped installed `skills/implementaudit/SKILL.md` for Stage 0 through Stage 7, embedded no-second-goal rule, and final markers. | done |
| Embedded governance does not emit nested `/goal` | Installed skill contained `do not print a second /goal` and Stage 7 only when not already embedded; `bash scripts/check-planner-stages.sh` enforces this. | done |
| Release asset install works into temp Codex home | `bash tests/release-asset-install.test.sh` and strict public `--tag v0.2.4.0` install smoke passed. | done |
| Checksum mismatch fails | `bash tests/release-asset-install.test.sh` mutates a stale checksum manifest and requires installer failure. | done |
| Final marker ordering | `bash scripts/check-marker-order.sh fixtures/simple-audit/EXPECTED-TRANSCRIPT-SKELETON.md fixtures/zero-optional-tool/COMPLETE-RUN.md` passed; `tests/marker-order.test.sh` rejects `AUDIT_HANDOFF` / `FAILURE_HANDOFF` before completion. | done |
| Read-only source-owner review | Subagent `019e9ac7-1129-7de1-a3f1-7085dc0453b0` inspected changed files, external inputs, README generated consistency, install claims, optional sidecars, comparator leakage, release/provenance wording, and version consistency. | changed: required doc/evidence patches applied |
| Adversarial false-pass review | Subagent `019e9ac7-3ad7-71a3-b5f2-41d7195d12cb` tried to falsify comparator absence, README truthfulness, generated README freshness, optional sidecars, install proof, passive-update wording, Stage 0-7 placement, public install proof, and marker exclusivity. | done |

## Release evidence

| Surface | Evidence | Status |
|---|---|---|
| Same-version release repair | The earlier public `v0.2.4.0` tag/release state was superseded for final closure by the forbidden-term, runtime-contract, and package-boundary cleanup. | done: `v0.2.4.0` is reissued from the cleaned tag target containing this ledger |
| Tag/source archive | `v0.2.4.0` peels to the cleaned source commit; GitHub source zip was downloaded and scanned. | done: source archive scan passed with the real forbidden term supplied externally |
| Release asset | `IMPLEMENTAUDIT.skill` was rebuilt from the cleaned source and scanned for externally supplied forbidden terms. | done: asset size `113775`, SHA-256 `da1980ec13a254d41568ea0ba79e86354f2d634950d71008c503adb5e85ab321`, extracted payload top-level paths `.claude-plugin` and `skills` only |
| Checksum manifest | `CHECKSUMS.txt` was regenerated for the cleaned `IMPLEMENTAUDIT.skill`. | done: checksum manifest names the same `IMPLEMENTAUDIT.skill` digest |
| Public release install | Strict tag-based smoke downloaded the cleaned public asset/checksum, verified checksum, installed into a temporary Codex-style skill home, and confirmed installed `SKILL.md`. | done: public install smoke passed |

## Remaining risk

- Host marketplace/plugin install behavior remains documented but not verified.
- Checksum manifest is bounded integrity evidence only; no signature,
  attestation, SBOM, license, marketplace verification, host install
  verification, package publication, or broader provenance claim is made.
- No Graphify install/indexing and no ActiveGraph setup/export occurred.
