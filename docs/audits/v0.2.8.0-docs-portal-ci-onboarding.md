# v0.2.8.0 Docs Portal, CI, and Onboarding — Audit Ledger

Run: direct governance  
Goal: close the public docs portal and onboarding gap for theIslampill/IMPLEMENTAUDIT.md  
Completed: 2026-06-08  
Base commit: d2829a4 (v0.2.8.0 release, committed in adaptation lane)  
Live portal: https://theislampill.github.io/IMPLEMENTAUDIT.md/

---

## Explicit required statements

The /goal required explicit statement of the following facts. All stated
here at the top of the ledger before any item classification.

**Local rough draft read?**
YES. `C:\Users\theis\Downloads\IMPLEMENTAUDIT_docs_portal_chronological_repair.html`
was read (lines 1–80) in this audit session. It is a design prototype with
different CSS variables (using `--bg-base: #0b1120`, gradient glows, etc.) from
what was ultimately generated. It was NOT used as a canonical source for the
portal generator. The generator (`scripts/build-docs-portal.py`) reads from
`docs/portal/onboarding.md`. The `rough_draft_used: false` field in
`docs-metadata.json` confirms this for every CI-built artifact.

**Pages workflow added?**
YES. `.github/workflows/pages.yml` was created and committed in commit `d2829a4`
as part of the v0.2.8.0 adaptation lane. The workflow: builds the portal with
`python scripts/build-docs-portal.py --out _site`, validates with
`python scripts/check-docs-portal.py _site`, uploads the artifact, and deploys
via `actions/deploy-pages@v4`. CI confirmed success after a race-condition
re-run (GitHub Pages API race on first push; re-run succeeded).

**Live Pages deployment actually verified?**
YES. Verified twice:
- Prior session (v0.2.8.0 release gate): HTTP 200,
  `commit_sha=d2829a4, plugin_manifest_version=0.2.8, project_milestone=v0.2.8.0,
  rough_draft_used=false`
- This session (2026-06-08): WebFetch HTTP 200,
  `commit_sha=d2829a4, project_milestone=v0.2.8.0, plugin_manifest_version=0.2.8,
  generated_at=2026-06-08T07:36:38.686363+00:00`

Evidence type: live runtime (live URL fetch, two independent checks).

**Release/tag/provenance in this run?**
NO release, tag, push, or provenance action was performed in this audit run.
The v0.2.8.0 release (commit `d2829a4`, tag `v0.2.8.0`, GitHub Release with
`IMPLEMENTAUDIT.skill` SHA256 `718075c053f5a5341168d9697d85ce4d95349dffb8b77885f3a142cabe3ec434`
and `CHECKSUMS.txt`) was separately authorized and executed in the prior
release gate session. This run creates only this audit ledger document.
No commit is authorized for this run; proposed commit message is in the
local git trace section at the end of this file.

---

## Objective

Close the public docs portal and onboarding gap. The adaptation lane (v0.2.8.0)
built all core deliverables; this ledger audits those deliverables, runs the
three additional checks not in the audit-fix checklist, re-verifies the live
portal, and provides terminal classification for every /goal item.

---

## Findings ledger

| # | Finding | Priority | Action | Status | Evidence |
|---|---|---:|---|---|---|
| 1 | Generator (build-docs-portal.py): stdlib-only, dark theme, 78ch prose, skip link, IntersectionObserver sidebar, 10 sections, `rough_draft_used: false` | P1 | Verify per-claim | done | `check-docs-portal.py /tmp/dp-verify` → ok (10 nav anchors, metadata valid); `build-docs-portal.py` → 10 sections, commit=d2829a4, milestone=v0.2.8.0 |
| 2 | Validator (check-docs-portal.py): 12-check suite covering all structural, metadata, no-forbidden-path, section-presence, and accessibility requirements | P1 | Verify per-claim | done | `docs-portal.test.sh` Test 2 → ok; direct run → ok |
| 3 | Test suite (tests/docs-portal.test.sh): 26/26 covering generator, validator, metadata, anchors, sections, package exclusion, negative test | P1 | Verify per-claim | done | `bash tests/docs-portal.test.sh` → 26/26 ok (re-confirmed 2026-06-08) |
| 4 | GitHub Pages CI (pages.yml): build+validate+deploy pipeline, OIDC permissions, concurrency guard, OWNER DECISION resolved via gh API `build_type=workflow` | P1 | Verify per-claim | done | CI run confirmed success; live portal HTTP 200; OWNER DECISION resolved programmatically |
| 5 | validate.yml wired: docs-portal.test.sh added to focused behavior test suite | P1 | Verify per-claim | done | `.github/workflows/validate.yml` contains `bash tests/docs-portal.test.sh`; confirmed in commit d2829a4 |
| 6 | Visual/readability smoke: no fixed-height cards, no overflow-hidden prose, 78ch width, skip link, IntersectionObserver, mobile responsive | P2 | Static + live | unverified | Static inspection of build-docs-portal.py: `--max-content: 78ch`, no `overflow: hidden` on prose, skip link present, IntersectionObserver wired. Browser tooling unavailable for live render check. Evidence type: manual inspection. Remaining risk: rendering on non-standard viewports, font-fallback edge cases. |
| 7 | Onboarding content (docs/portal/onboarding.md): 10 sections, all required topics, neutral comparator language, no comparator proper name | P1 | Verify per-claim | done | All 9 required anchors confirmed by `docs-portal.test.sh` Tests 8.1–8.9; comparator section uses generic language ("generic staged-goal runner", "external staged-goal comparator"); no all-domain obsolescence claim |
| 8 | Live Pages deployment verified (HTTP 200, metadata matches release commit) | P1 | Live fetch | done | WebFetch 2026-06-08: HTTP 200, commit_sha=d2829a4, project_milestone=v0.2.8.0, plugin_manifest_version=0.2.8, generated_at=2026-06-08T07:36:38Z |
| 9 | Dedicated docs portal audit ledger (this file) | P1 | Create | changed | File created in this audit session: docs/audits/v0.2.8.0-docs-portal-ci-onboarding.md |
| 10 | check-host-claims.sh (not in audit-fix checklist) | P1 | Run check | done | `bash scripts/check-host-claims.sh` → ok (2026-06-08) |
| 11 | generate-readme-diagrams.sh --check (not in audit-fix checklist) | P1 | Run check | done | `bash scripts/generate-readme-diagrams.sh --check` → ok (2026-06-08) |
| 12 | check-readme-toc.sh (not in audit-fix checklist) | P1 | Run check | done | `bash scripts/check-readme-toc.sh` → ok (2026-06-08) |

All 12 items terminally classified. 0 open.

---

## Scope-creep register

| # | Issue | Location | Recommendation |
|---|---|---|---|
| SC-1 | `workflow_dispatch` trigger absent from pages.yml — only `push: branches: [main]` is wired; cannot manually trigger portal rebuild | `.github/workflows/pages.yml` line 11 | Add `workflow_dispatch: {}` to the `on:` block; out of scope for this run |
| SC-2 | Test 7 in docs-portal.test.sh reports "(1 found)" nav anchors when checking; the authoritative `check-docs-portal.py` correctly finds 10. The bash inline Python regex `nav-link[^>]*href="#..."` does not match the generated HTML order (`href` before `class="nav-link"`); the fallback regex matches correctly but the count display appears to reflect only 1 group. The test condition checks `> 0`, so it passes. | `tests/docs-portal.test.sh` Test 7 | Fix the nav anchor count display in Test 7's inline regex to accurately reflect all 10 anchors; or delegate count-check to check-docs-portal.py; out of scope for this run |

---

## Phase-by-phase evidence (docs portal scope)

### Generator and validator (Items 1–2)

- `scripts/build-docs-portal.py`: reads `docs/portal/onboarding.md` as primary
  source; reads `README.md`, `CHANGELOG.md`, `docs/audits/INDEX.md`,
  `.claude-plugin/plugin.json` for metadata. Stdlib only. Produces
  `index.html` + `docs-metadata.json`. `rough_draft_used` field hardcoded
  `False` — generator never reads the rough draft HTML.
- `scripts/check-docs-portal.py`: 12 checks. Skip link check (`class="skip-link"`);
  semantic headings (`h1` or `h2`); nav anchors unique and document-ordered;
  9 required section anchors; 8 required metadata fields; `rough_draft_used: false`;
  no `file:///` URLs; no absolute Windows paths; no forbidden overclaims.
- Evidence: `python scripts/build-docs-portal.py --out /tmp/dp-verify` →
  `10 sections, commit=d2829a4, milestone=v0.2.8.0`;
  `python scripts/check-docs-portal.py /tmp/dp-verify` →
  `ok (10 nav anchors, metadata valid, all checks pass)`.

### Test suite (Item 3)

`bash tests/docs-portal.test.sh` → `26/26 ok`.

Breakdown:
- Test 1: generator exits 0
- Test 2: `check-docs-portal.py` passes
- Tests 3–4 (×10): metadata fields, rough_draft_used false
- Tests 5–6: no forbidden URLs/paths
- Test 7: nav anchors present (count display has SC-2 quirk; passes `> 0` check)
- Tests 8.1–8.9: all 9 required section anchors present
- Test 9: `verify-package.sh` still passes (dist/ excluded from .skill package)
- Test 10: negative test — generator writes to any writable output dir

### GitHub Pages CI (Item 4)

- `.github/workflows/pages.yml`: created, committed in d2829a4, pushed to main.
- OWNER DECISION resolved: GitHub Pages `build_type=workflow` configured via
  `gh api -X POST repos/theislampill/IMPLEMENTAUDIT.md/pages -f build_type=workflow`.
- CI first run failed (404 race condition on Pages API creation). Re-run with
  `gh run rerun 27122873513 --failed` succeeded.
- Live portal HTTP 200 at https://theislampill.github.io/IMPLEMENTAUDIT.md/
  with correct commit metadata confirms deployment is live.
- Missing: `workflow_dispatch` trigger (scope-creep SC-1).

### validate.yml integration (Item 5)

`bash tests/docs-portal.test.sh` is present in `.github/workflows/validate.yml`
in the focused behavior tests section. Confirmed in commit d2829a4.

### Onboarding content (Item 7)

`docs/portal/onboarding.md` — 10 onboarding sections:

| Section | Anchor | Status |
|---|---|---|
| What is IMPLEMENTAUDIT? | `#what-is-implementaudit` | present |
| When should I use it? | `#when-should-i-use-it` | present |
| When should I not use it? | `#when-should-i-not-use-it` | present |
| Normal prompt vs /goal vs IMPLEMENTAUDIT | `#normal-prompt-vs-goal-vs-implementaudit` | present |
| Three invocation modes | `#three-invocation-modes` | present |
| Compared with a generic staged-goal runner | `#compared-with-a-generic-staged-goal-runner` | present |
| What Graphify and ActiveGraph do | `#what-graphify-and-activegraph-do` | present |
| What completion means | `#what-completion-means` | present |
| Install and update | `#install-and-update` | present |
| Easy invocation examples | (10th section, not in 9-anchor required list) | present |

Comparator language check: The "Compared with a generic staged-goal runner"
section uses: "generic staged-goal tools", "generic staged-goal runner",
"external staged-goal comparator" pattern. No comparator proper name, slash
command, artifact path, phase marker, or memory marker embedded. No
all-domain obsolescence claim. Each tool scope statement: "No all-domain
obsolescence claim is made. Each tool has its own scope and users."

### Additional checks (Items 10–12)

All three checks not included in the adaptation lane's audit-fix checklist,
run explicitly in this session (2026-06-08):

```
bash scripts/check-host-claims.sh           → check-host-claims: ok
bash scripts/generate-readme-diagrams.sh --check  → generate-readme-diagrams: check ok
bash scripts/check-readme-toc.sh            → check-readme-toc: ok
```

Evidence type: live runtime (local shell, current working tree).

---

## Smoke B (post-implementation, this session)

All checks run after the adaptation lane was already complete. This session
confirmed the released state without mutation.

| Check | Command | Result | Evidence type |
|---|---|---|---|
| Generator | `python scripts/build-docs-portal.py --out /tmp/dp-verify` | 10 sections, commit=d2829a4, milestone=v0.2.8.0 | local generated-runtime |
| Validator | `python scripts/check-docs-portal.py /tmp/dp-verify` | ok (10 nav anchors, metadata valid) | local generated-runtime |
| Test suite | `bash tests/docs-portal.test.sh` | 26/26 ok | unit test |
| Host claims | `bash scripts/check-host-claims.sh` | ok | static checker |
| Diagrams | `bash scripts/generate-readme-diagrams.sh --check` | check ok | static checker |
| README ToC | `bash scripts/check-readme-toc.sh` | ok | static checker |
| Live portal | WebFetch https://theislampill.github.io/IMPLEMENTAUDIT.md/ | HTTP 200, commit_sha=d2829a4, v0.2.8.0 | live runtime |
| Visual/readability | Static inspection of build-docs-portal.py HTML output | 78ch, skip link, IntersectionObserver, no overflow-hidden | manual inspection (unverified for live render) |

---

## Plan closure

| # | Item | Status | Follow-up |
|---|---|---|---|
| 1 | Generator (build-docs-portal.py) | done | — |
| 2 | Validator (check-docs-portal.py) | done | — |
| 3 | Test suite (26/26) | done | — |
| 4 | GitHub Pages CI (pages.yml) | done | SC-1: add workflow_dispatch (separate run) |
| 5 | validate.yml wired | done | — |
| 6 | Visual/readability smoke | unverified | Browser tooling unavailable; static inspection confirms structural requirements |
| 7 | Onboarding content (10 sections, neutral language) | done | — |
| 8 | Live Pages deployment verified | done | — |
| 9 | Dedicated audit ledger (this file) | changed | Propose commit (see below) |
| 10 | check-host-claims.sh | done | — |
| 11 | generate-readme-diagrams.sh --check | done | — |
| 12 | check-readme-toc.sh | done | — |

---

## Graphify-assisted Gemba

Graphify was available in the prior session (v0.2.8.0 adaptation lane) and used
for terrain queries against pre-existing TEMP graph artifacts (3/5 graphs). In
this session, Graphify was not queried — live-file Gemba was used directly
(read files, run checks). No new Graphify indexing was performed.

---

## Local git trace

No commit was authorized for this audit run. No push, tag, release, or
provenance action was performed.

**Proposed commit message** (for owner to apply when ready):

```
docs: add v0.2.8.0 docs-portal-ci-onboarding audit ledger

Finding:
- /goal for "close the public docs portal and onboarding gap" required a
  dedicated audit ledger separate from v0.2.8.0-adaptation.md. Three
  checks (check-host-claims, generate-readme-diagrams --check,
  check-readme-toc) were not in the adaptation lane checklist.

Countermeasure:
- Created docs/audits/v0.2.8.0-docs-portal-ci-onboarding.md: terminal
  classification for all 12 ledger items; explicit statements for rough
  draft read, pages.yml added, live deployment verified, no unauthorized
  release/tag/provenance.
- Run deferred checks: check-host-claims ok, generate-readme-diagrams
  --check ok, check-readme-toc ok.
- Re-confirmed live portal HTTP 200, commit_sha=d2829a4, v0.2.8.0.

Smoke B:
- docs-portal.test.sh 26/26 ok
- check-docs-portal.py /tmp/dp-verify: ok (10 nav anchors, metadata valid)
- check-host-claims.sh: ok
- generate-readme-diagrams.sh --check: ok
- check-readme-toc.sh: ok
- Live portal WebFetch: HTTP 200, commit_sha=d2829a4, v0.2.8.0

Scope-creep (not implemented):
- SC-1: workflow_dispatch missing from pages.yml
- SC-2: Test 7 nav-anchor count display shows 1 instead of 10

Boundaries:
- No commit, push, tag, release, or provenance in this run.
- v0.2.8.0 release was separately authorized and already published.
```

---

## Capability Ledger

ActiveGraph was configured for the adaptation lane (v028-adaptation-b3lU0C/custody.db,
19 events). This separate docs portal audit run was not wired to ActiveGraph.
Markdown ledger is the first-class record for this run.

Capability entries (Markdown fallback):

- Confirmed live portal deployment at commit boundary (d2829a4, v0.2.8.0),
  two independent checks (HTTP 200).
- Ran three deferred checks (host-claims, diagrams, README ToC) — all ok.
- Identified two scope-creep items (workflow_dispatch, test 7 count) without
  implementing them.
- No broad competence claim is made from one run.

---

## Remaining caveats

1. **Visual/browser smoke (Item 6 — unverified)**: Static inspection confirms
   78ch prose width, skip link, IntersectionObserver active sidebar, no
   fixed-height cards, no overflow-hidden prose, mobile breakpoint at 700px.
   Live browser rendering on non-standard viewport sizes, with font-fallback
   stacks, or on high-DPI displays has not been tested. This is the only
   unverified item.

2. **workflow_dispatch absent (SC-1)**: pages.yml only triggers on push to main.
   A manual portal rebuild requires a push. This is not a correctness issue
   but reduces operational flexibility.

3. **Test 7 nav-anchor count (SC-2)**: Test 7 in docs-portal.test.sh reports
   "(1 found)" due to a regex order mismatch in the bash inline script; the
   authoritative `check-docs-portal.py` correctly finds 10. The test passes
   because it checks `> 0`. The count display is misleading but not a
   functional regression.

4. **AGENTS.md update**: Not updated in this run. No new anti-repeat rule
   emerged from this run that would have prevented a prior mistake. The
   durable rules from the adaptation lane (V0280-PY-USE-STDOUT-WRITE,
   recursive verify-package loop) remain in AGENTS.md from d2829a4.
